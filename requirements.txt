# Host / desktop training (Orin JetPack packages differ — install TensorRT from JetPack)
ultralytics>=8.3.0
torch>=2.0.0
torchvision
opencv-python>=4.8.0
numpy>=1.24.0
PyYAML>=6.0
tqdm>=4.66.0
Pillow>=10.0.0
matplotlib>=3.7.0
pandas>=2.0.0
seaborn>=0.13.0
psutil>=5.9.0
gdown>=5.0.0
lxml>=4.9.0
rich>=13.0.0

# Jetson power logging (optional on host)
# jetson-stats  # jtop — install on Jetson: sudo pip install -U jetson-stats
