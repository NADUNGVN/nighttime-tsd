# CCTSDB2021 night subset for cross-dataset evaluation
# Built by: python scripts/extract_cctsdb_night.py
# Labels: 3 classes (prohibitory / mandatory / warning)

# Only used for val/test (models trained on CNTSSS).
# train=images so Ultralytics yaml validates; do not train on this file.
path: ../data/processed/cctsdb2021_night
train: images
val: images
test: images

names:
  0: prohibitory
  1: mandatory
  2: warning

nc: 3
