# ST-SERVER-01 R3 — production path still not covered

2026-09-22, reviewed b0ef5c9877ac4255c9f80335303c514304de0621. **NO-GO 84/78 execution; GO finish the same local work package.** This is not a new protocol or experimental requirement. R2 closure claims are only partly supported. Keep repaired schema/mapping/FP16/schedule work; do not restart it or change the scientific design.

## Evidence and test interpretation

Astra independently ran the super suite with `D:/Research/paper/local/measurement_audit_env/Scripts/python.exe`: **15/15 PASS**, zero skips. The 84/78 test still calls `runtime_double`, which fabricates completed build/capture/counter/matching states and never executes `build_real`. Its analyzer test replaces `coco_size`, `ap_values` and `resample_ap` with constants. This proves orchestration/table assembly, not the claimed production child/capture/numerical integration. Do not relabel it as external-boundary coverage of code it bypasses.

## Immediate reproducible blockers

1. **No-op warmup is counted as forbidden inference.** `CountingNoWarmupBackend.warmup` increments `warmup_calls`, while production capture and parent demand zero. Installed pinned Ultralytics validator calls `model.warmup` unconditionally in the standalone path. Astra extracted the exact class AST, instantiated it with a CPU dummy base and called warmup: `warmup_calls=1`, `total_calls=0`; the production check rejects this. Distinguish skipped warmup requests from actual warmup forwards, require actual forwards zero, and test the actual validator/backend integration. Do not permit an extra forward to solve this bug.

2. **GPU identity pin is lost.** Parent sets `manifest['gpu_identity']` after validating a child, then unconditionally reads the manifest again before append/write, discarding the new binding. Later children therefore never compare to the persisted first identity. Save the identity and job atomically together and reject missing identity in production. Test different UUID/driver on later jobs, including absence of identity and failure before any further job. Scope comparison to the approved model/host policy; do not alter device state.

3. **Lifecycle completion is asserted before release.** `CountingNoWarmupBackend.last_instance` retains the backend/context/engine after `del validator`; Astra confirmed the class holds its instance. Builder/config/network/calibrator references also remain live when status `released` is written. `synchronization_completed=1636` is still assigned rather than counted at observed completion boundaries, and failures publish no attempted/completed forward counters. Use explicit ownership/finally cleanup (including class references and restoring patched globals on constructor failure), durable attempts/completions, and separate sync/release evidence. Do not convert process exit or a final synchronize into an invented per-forward counter. Preserve primary error and all cleanup errors, with bounded owned-child/descendant termination on timeout.

4. **XML location is hardcoded incorrectly for the documented command.** Plan uses `data/raw/CCTSDB2021/xml.zip` under nighttime-tsd-new, while runbook analysis uses `/home/ubuntu/Dung_TDTU/nighttime-tsd/data/raw/CCTSDB2021/xml.zip`. There is no XML CLI binding/preflight before builders. Accept an explicitly resolved read-only XML input and verify canonical hash/membership/2706 instances before dispatch. Do not discover the missing file after consuming builds. Carry its binding into every capture/analyzer. Do not relocate or overwrite data to hide the mismatch.

## R2 items not actually closed

- **Frozen bytes:** child does not rehash ONNX/checkpoint before/after use; it records plan values. Calibration YAML/source-byte hashes are copied from producer evidence without rechecking current bytes. Scored cache compares before vs after itself, not against the auxiliary cache's pinned model/selection hash on every read. Make the actual execution identity chain enforceable, and test changed YAML/input/cache/ONNX between plan and dispatch. Validate immutable config/helper/graph bindings, not just stated hash fields. All inputs may be checked read-only on CPU before runtime import.
- **Capture semantics and metadata:** engine is still raw serialized bytes passed to framework engine reader. Demonstrate packaging/metadata compatibility with the exact pinned backend without adding model calls; freeze names, stride and v26 end-to-end handling. Use the real postprocess on saved synthetic v8 and v26 outputs and verify counts/classes/coordinates. `postprocess_route` written from the plan is not evidence that the route was followed. `replay_statistics` is computed but not compared with actual validator metrics; add the existing accepted replay comparison and report outcome. A count of 1636 unique images is not canonical dev identity; bind exact IDs/shapes and XML/source bytes.
- **Precision evidence:** values read from `layer.precision/get_output_type` before build are requested network constraints, not effective engine precision. Rename them accurately. Publish sanitized inspector evidence/hash, keep arithmetic coverage unknown when not established; do not infer actual INT8 fraction from flags. Engine inspector currently remains private-only with no public content/hash chain in the declared allowlist.
- **Analyzer acceptance:** `_cell_files/analyze` still accepts `synthetic_external_runtime_double` as production input. It does not audit child cache/telemetry/owner/counter evidence, engine hash against child state, XML hash against canonical/each capture, or recompute native matching and metric replay. The synthetic full-analyzer test has zero GT yet returns a summary declaring 2706 instances because that count is a literal. Reject invalid scientific inputs; keep explicitly labeled synthetic test mode/results separate from production. Use real pooled evaluator and bootstrap on a small synthetic dataset to verify actual numerical integration; mocking them to constants is not sufficient. Retain full canonical-size production checks.
- **Publication/runbook:** implement the promised public allowlist/failure inventory, disk/resource requirements and actual point/CI/variation/replay/manuscript tables. Do not describe a list in prose as generated publication evidence. The honest single-host limitation is accepted for this implementation; no need to add multi-host merging to resolve R3.

## How to finish without another scaffold-only review

Keep one production state machine. Inject doubles at the external builder, device memory/stream/backend and telemetry boundaries, not by selecting a second function that fabricates terminal states. Tests must execute that same calibration binding, actual callbacks, requested target handling, capture/validator semantics, owner cleanup and analyzer checks. A small injected tensor/dataset fixture is acceptable if test mode is explicitly separated from production and cannot publish scored results. No actual local CUDA import, TensorRT, frozen model or ORT forward is needed.

Mandatory acceptance tests in the consolidated packet:

- Actual production wrapper receives a framework warmup request, performs zero warmup forward, then counted data calls; failures retain partial counts and release owners.
- Real producer output is consumed by real child preparation for auxiliary, FP16 and each INT8 arm on both architectures; wrong byte/selection/model/UUID/runtime/metadata fails before mocked build.
- Persisted UUID rejects a changed second job; no subsequent dispatch.
- Cache reuse is linked to the auxiliary hash; requested constraints and inspector evidence remain distinct.
- Complete 84/78 production-path simulation, including all phase transitions; no fabricated terminal lifecycle shortcuts.
- Actual native/COCO numerical replay on synthetic captures with known positives/false positives, duplicate-image bootstrap, point reproduction/undefined draws and JSON publication. Production rejects synthetic evidence and wrong dataset/XML/matching/cleanup counters. At least one full analyzer success test uses the real evaluator.
- Error/timeout/cleanup and immutable-input mutation cases reach the specific expected guard, not an unrelated earlier invalid fixture. Show coverage/repro evidence, not just assertions on counts.

Use L2A-053 milestones and a closure table for every numbered/carry-forward item. Correct earlier claims where tests bypassed production. Commit/push unchanged A2L-048/R3 with scoped work through NADUNGVN; preserve dirty checkpoint and old artifacts. Finish and self-review the whole package before resubmission. NO server command or GPU run before integrated GO; no extra canary or change to the original study budget.
