# Astra to Luna 1 — Edge deployment lane

## E2L1-001 — GO SSH read-only inventory and local harness preparation

### Context and ownership

Paper: Edge INT8 Traffic-Sign Detection, CCTSDB2021 three classes. Fifteen trained FP32 checkpoints are frozen; no retraining or external training datasets. Original VCSC superiority hypothesis was not supported. Current question: accuracy loss and detection-head precision sensitivity, versus deployment cost. YOLO11n dev diagnostics found improvements from head FP32 protection, but this is not yet cross-model or edge evidence; do not advertise universal speedup or accuracy guarantees.

Main Luna is implementing graph preparation for YOLOv8n and YOLO26n confirmation (3 Uniform selections, 4 INT8 arms, 3 builds plus FP16). This is independent of edge inventory. Main Luna cannot SSH into servers; the user is server operator. **Luna 1 may SSH into the user's designated edge devices for the bounded read-only task below**, because user has confirmed they are ready and accessible. Do not use or discover unrelated hosts/accounts.

### Access source supplied by the user

Read `D:/Research/Teacher_Vu/docs/SHARED_INFRASTRUCTURE.md`, especially Sections3.1/3.2, for existing SSH user/host mappings. This is a local shared-infrastructure reference, not a file to copy into this paper repository. Do not ask the user to repeat endpoints already documented. Use existing authorized keys/config; ask only for missing key/alias access details or workspace permission, never private key/password contents. The recorded addresses/versions are historical observations, not live reachability evidence. Do not disable SSH host-key verification.

Inventory targets mapped from that reference:

| Infra ID | Device / access section | Role in current task |
|---|---|---|
| E1 | Raspberry Pi5 + Hailo-8; `urlab`, Tailscale endpoint in E1 table | GO read-only inventory. CPU-only and Hailo may be two runtime conditions on this same physical board, not two independent devices. No accelerator disabling or hardware removal needed for inventory. |
| E2 | Xavier NX; `arar`, LAN endpoint in E2 table | GO read-only inventory. |
| E3 | AGX Xavier; `arar`, LAN endpoint in E3 table | GO read-only inventory; verify exact RAM/SKU instead of assuming32/64GB developer kit. |
| E5 | Orin Nano Super; `huy`, Tailscale endpoint in E5 table | GO read-only inventory as available candidate hardware; **not legacy Jetson Nano** and not silently substituted into paper scope. Await user/reviewer choice before scored study. |
| Legacy Nano | Not identified in supplied infrastructure file | Missing mapping, not a failed SSH host. Do not scan networks to find it; ask operator whether present. |
| Separate Pi5 CPU board | Not identified separately | Record unknown; may reuse E1 CPU condition after protocol approval, do not claim a fifth independent physical board. |

E4 RUBIK Pi3/Qualcomm is documented for another project but **out of this task's scope**; do not add it automatically. Existing segmentation/audio compiler smoke results in the source document are not evidence for CCTSDB YOLO model compatibility or measured performance. Do not modify those projects' environment/output directories or reuse their engines/calibration as ours. Existing server SSH information does not change main Luna's operator-mediated server workflow.

### GO now

1. Inspect existing edge/benchmark code in repo before creating another implementation. Use a dedicated branch and separate worktree/clone if agents share filesystem. Proposed owned paths: `scripts/edge_readiness/`, `tests/test_edge_readiness.py`, `docs/EDGE_READINESS_V1.md`, `docs/LUNA1_TO_ASTRA_EDGE.md`, `results/edge_readiness_v1/<device_id>/<run_id>/`. Coordinate reuse without editing main Luna's confirmation files. No broad staging or branch switching in another agent's working tree.
2. Create a small schema-versioned read-only collector and CPU fixture tests; reuse scripts when suitable. Run inventory via SSH once local tests establish commands have no mutating flags. No need to wait for source-model confirmation. Record actual commands, exit codes, timestamp, code hash and unknown/unavailable/permission-denied states rather than fabricating compatibility.
3. Collect available hardware model, RAM, CPU architecture, GPU/accelerator identity, OS/kernel, installed JetPack/L4T/TensorRT/CUDA/driver/Python/runtime package versions as relevant, disk availability, existing power mode/clocks, idle thermal state, cooling/power-supply description (operator supplied where unobservable). No assumption a device must support server TensorRT10.16.1.11. Missing tool is a reportable prerequisite, not authorization to install.
4. Check availability of existing telemetry sources and power measurement: describe onboard/software/external meter, measurement boundary (chip/module/whole device), units, sampling limitations. A short bounded read-only sample is allowed; no stress test, inference loop, clock/power-mode change, or calibration. Do not equate nominal TOPS/TDP with measured power or throughput.
5. Produce compatibility matrix separating observed installed capability from documentation/inference/unverified export support. For version-specific support conclusions consult official vendor documentation and record source/version; do not decide compatibility from model name alone. Record likely risks for native YOLO26 end2end output, precision constraints, operator coverage, and backend conversion without forcing unsupported paths.
6. Draft a benchmark harness interface/protocol and CPU mocked tests only: future manifest binds source-weight hash, calibration IDs/hash/order, device/backend-native quantization, runtime versions, preprocessing/postprocessing, batch/input shape, warmup/measured counts, latency boundary, synchronization, percentiles, memory, measured-power boundary and thermals. Mark counts/backend options as proposed until reviewed; do not silently copy server timings into edge results.

### NO-GO without separate review

- No training, model download, ONNX export, TensorRT build, Hailo compilation/calibration, scored inference or benchmark matrix.
- No apt/pip/firmware/OS changes, sudo mutation, reboot, service stop, process kill/pause, power/clock/fan changes or automatic dependency repair.
- No source-weight/calibration resampling, policy selection, official-test-guided tuning, or changes to main confirmation protocol.
- Do not copy RTX8000 engine to an edge and treat it as a portable model. Cross-backend comparison means **same frozen source weights + same calibration image IDs where logically applicable + backend-native quantization**, not numerically identical INT8 engines. CPU FP32/other unsupported precision can be legitimate reference; never relabel mixed precision as all-INT8.

Missing privileges/devices should block only that device's affected check. Continue read-only checks on other authorized devices and document limitations. Do not wait for all mapped devices or unresolved legacy-Nano mapping before reporting the first useful inventory; do not expand access to make them pass.

### Deliverables and gates

- Device inventories JSON + concise report, machine-readable compatibility matrix, local tests, proposed measurement protocol and dependency/action list with scope/risk. New output directories, no overwrite. Keep host credentials/private network addresses out of committed public artifacts; use stable device aliases and retain hardware identity bindings in the agreed private artifact scope if sensitive.
- Append **L1A-001** to `docs/LUNA1_TO_ASTRA_EDGE.md` with actual contacted device aliases, evidence, incomplete checks, local tests, commands and Git commit. Preserve this instruction file and commit it with scoped work. Push through authorized Git account; tell Astra which branch/commit, do not merge main Luna changes blindly or force-push.
- Astra reviews inventory and proposed setup before authorizing installations/conversion/smoke tests. **GO inventory/preparation now; NO-GO scored edge study.** Final representative model/arm/backend selection waits for confirmation review, but setup planning need not wait.

Completion is honest device readiness evidence, not successful deployment or paper performance results. These are parallel work packages, not permission to bypass scientific gates.

## E2L1-002 — accept inventory snapshot; GO local measurement harness/protocol

Reviewed branch `luna1/e2l1-001-edge-readiness`, commit `5d407f1a9c285d274c9009ac99511afb539817c4`, L1A-001. Astra inspected 6 canonical JSON artifacts (4 device inventories + compatibility/protocol), each device records 24 commands and SSH transport exit 0. Collector hash in all 4 inventories matches canonical collector source `b9abe9272cae4c015d6094248e559f854f417840b9ac1abe877745b6ed87b212`. Full combined suite 166/166 passed, including 5 edge tests. This confirms recorded provenance/structure, not independent re-SSH execution; unknown/permission-denied checks remain unknown. Accept as **initial live inventory**, not YOLO deployment support or performance validation.

Observed useful constraints: E1 root free 4.9GB (83% used), E2 free 94GB, E3 free 11GB, E5 free 792GB. E5 is Orin Nano Super, not legacy Nano. No cleanup/install/clock modification authorized. Existing dependency and board observations can guide planning, but documentation-derived JetPack labels remain separate from observed L4T/package versions. External power meter/cooling/supply boundary still unresolved; no actual telemetry sample recorded. Do not treat inventory temperatures as controlled idle without workload evidence.

### Next independent task (does not wait for Luna graph)

1. Draft concrete measurement protocol and implement backend-neutral **local CPU/mock harness**, no actual device inference/build. Reuse existing project latency/statistics helpers where appropriate; preserve old accepted results. Include explicit inference-only versus decoded-image-to-detections boundaries, synchronization responsibility per backend, preprocessing/postprocessing, batch 1/input 640, warmup/measurement/session design, device-native model/hash bindings and negative/error states. Propose 200 warmup/1000 measured samples × 3 sessions as starting design consistent with prior study, clearly pending review and thermal/power sampling suitability; preserve per-sample/session data, distinguish FPS reciprocal latency versus measured pipelined throughput, no pooled 39k-independent-samples claim. Cross-device parser implementations/quantization need future correctness checks, not just timing.
2. Define measured power/energy integration with time-aligned samples and actual boundary; report unavailable rather than infer watts from TDP/TOPS. Memory metrics backend-specific and unit-labeled. Thermal/throttle and current power-mode observations before/after; do not prescribe max clocks or change modes without operator approval. Physical E1 CPU versus Hailo conditions may be proposed on same board, not two independent devices; no E5 scored scope substitution without confirmation.
3. Publish a setup action matrix for E1/E2/E3/E5: current observed prerequisites, missing packages/tools, proposed actions, disk budget, offline/compiler host needs, permission/risk. All installs/conversions remain proposed only. Prioritize E2/E3 GPU-native path and E1 CPU/Hailo feasibility planning; do not add DLA/Qualcomm or all 15 models. This is prioritization for planning, not final model/arm selection.
4. Harden collector for future use without rerunning all devices: validate SSH alias as safe non-option alias (current check permits leading `-`), reject duplicate/unexpected/missing command markers before declaring complete; keep transport success separate from inventory completeness. Preserve timeout partial evidence; `trtexec --help | head` exit 0 currently reports wrapper success, not reliable executable exit code—document existing record limitation and fix future collection if code touched. Use exclusive no-overwrite writer, source/script hash and tests. None of these findings alone invalidates the observed 24-label snapshots.
5. Append **L1A-002** with files/tests/protocol/options and outstanding operator questions. Push scoped branch changes, keep main-lane files unchanged. Don't open/merge PR automatically; supplied `/pull/new/...` is only creation URL.

### Concurrency safeguard

Astra found shared cwd on edge branch after Luna main work. Use a separate worktree/clone for each lane before further implementation; no checkout/reset in other's active workspace. This new instruction is currently unstaged here; preserve it when setting up isolation. Main-lane `docs/ASTRA_TO_LUNA.md` edits belong to Astra/Luna main, not edge commit.

GO local harness/tests/protocol and existing bounded read-only checks if needed. NO-GO deployment installation, export/compile, scored inference/latency/power study. Do not repeatedly collect unchanged inventory while waiting; move to useful implementation/design work. Main graph review and edge harness review can proceed independently.

## E2L1-003 — review bc611c9; fix energy math before real telemetry

Reviewed `bc611c9d8c58ba0aa5234308b4b29c415a48ba36` / L1A-002. Astra independently reran15/15edge testsPASS. Accept local/mock-only separation, named measurement boundaries, raw latency/session storage and collector hardening as preparation. **GO bounded local fixes below; NO-GO real benchmark/conversion/install remains.** Main Luna receives independent CPU ONNX collection authorization; do not wait for its graphs to fix harness.

### E1 — reproduced wrong energy results

`integrate_power_energy` currently labels partial overlap as measured full interval, and when clipping a segment uses original endpoint powers rather than interpolating powers at clipped boundaries.

Astra reproductions (timestamps in seconds, code inputs ns):

- requested[0,10], samples(2,10W),(8,10W) => returns `measured`,60J. Must not claim full-session energy: missing coverage[0,2]and[8,10]. Return unavailable/partial with coverage metadata, no full averagepower/Jperimage.
- linear samples(0,0W),(10,10W), requested[0,2] => returns10J; correct trapezoidal clipped integral is2J (boundary powers0Wand2W).

Require strictly increasing finite timestamps or explicit validated sorting policy; reject duplicate/conflicting timestamps, invalid/NaN/inf/negative power, mixed boundaries/units and unverified clock alignment. Full measured result needs samples bracketing entire interval. Interpolate power at clipped interval endpoints, don't extrapolate missing ends. Record coverage, duration, clock identity/alignment source, sample gap limitations and integration method; do not invent a validated max-gap threshold after observing real results. Tests full constant/ramp, clipped ramp, partial-left/right/interior, no overlap, duplicate/out-of-order, mixedboundary/nonfinite. If averagepower/energyperimage are added, denominator is covered full-duration/imagecount with exact time boundary, not reciprocal latency.

### E2 — measurement binding and persistence

`run_session` measures energy over outer session wall time including Python/provider overhead whereas latency excludes provider; persist absolute monotonic start/end/session duration and label energy boundary separately. Postprocess may be asynchronous for a future adapter: declare CPU synchronous or provide final synchronization before ending decoded-image timer. Make boundary enum validation explicit; unknown strings cannot silently choose end-to-end via else branch. Bind pool IDs/hash/order/size instead of hidden `%256` assumption for real adapters; mock may retain explicit mockpool.

Use atomic/exclusive no-overwrite open (`x`) in harness writer; current exists+write_text has a race. Preserve mock-only provenance and don't let config claim pipelined-throughput while harness reports serial. Future actual model/backend/device bindings must be checked, not self-declared labels only; no actual runtime adapter needed in this task.

### Handoff

Luna1 adds tests, updates measurement protocol with corrected integration and timing/clock scope, records **L1A-003**, commits/pushes scoped edge branch. Do not rewrite existing mock or inventory artifacts; new mock outputs if needed. No newSSH inventory required; no device inference or power-mode modification. Exact SKU/meter/cooling/operator questions remain open but do not prevent local math fixes. Astra leaves this entry unstaged in the isolated edge worktree for Luna1 to commit.

## E2L1-004 — accept integration math fixes; close actual session/pool/clock bindings

Reviewed `51bbecafa4420c1b1f5decd3a1dc80042db784f9` / L1A-003 on 2026-09-16. Astra independently reran **18/18 PASS** with `python -m unittest discover -s tests -p 'test_edge*.py' -v`. The slash-module invocation reported by Luna did not resolve the local `tests` namespace in Astra's environment; discovery did, without code changes. Accept clipped trapezoid correction (2J ramp case), partial coverage labeling, input-order checks, boundary enum validation and exclusive writer. No actual edge runtime/telemetry/energy measurement was performed by Astra.

**Decision: GO bounded local completion below; NO-GO scored/device inference or benchmark.** These are concrete contract failures, not a reason to wait for main Luna's graph. Do not rewrite earlier mock or inventory artifacts.

### E3 — measured image sequence does not use declared pool

Warmup now uses `pool_indices`, but measured calls still use `image_provider(index % 256)` at `run_session`. Astra reproduction: pool IDs `(a,b)`, order `(1,0)`, warmup=2, measured=4 gives provider calls `[1,0,0,1,2,3]`; required sequence with independent phase restart is `[1,0,1,0,1,0]`. Current output claims pool size 2 while calling indices 2 and 3 outside that pool.

Use declared order/size in both loops, explicitly define phase restart vs continuation, and persist/check actual consumed IDs/order or a deterministic sequence hash. Test non-256 pool, reversed order, wraparound and a provider that raises for out-of-range indices. Validate unique IDs and integer non-boolean permutation indices. Mock ID hash remains mock-only; future actual image-content provenance must not be replaced by hashing IDs alone.

### E4 — warmup included in energy, missing measured-window evidence

`session_start_ns` is captured before warmup and is passed to energy integration; `measured_start` is unused. `warmup_in_timing=false` is true for latency but not this energy interval. This risks later dividing warmup-inclusive joules by only measured calls.

Persist total-session window **and** post-warmup measured-loop outer window. The primary energy interval for measured-image energy must use measured-loop start/end, including provider/Python overhead for those calls but excluding warmup. Label `includes_warmup=false`, image count and distinct latency/energy boundaries. Optional warmup-inclusive energy must be separately named with its own count/window, never silently mixed. No need to add an energy-per-image statistic yet. Add deterministic mocked-clock tests with a large warmup duration to prove exclusion; do not rely only on wall-time greater than zero.

### E5 — clock consistency within samples is not binding to session clock

Astra reproduced `integrate_power_energy(rows, 0, 10, clock_identity='session-clock', alignment_source='same-process')` returning `measured` while both rows declare `clock_identity='other-clock'`. The keyword currently acts as a default and silently loses to row metadata. `run_session` supplies no expected clock identity at all. Equal strings among rows do not establish alignment to `perf_counter_ns` boundaries.

Define explicit requested/session clock binding. If an expected clock/alignment is supplied, contradictory sample metadata must fail; if conversion is needed, require an explicit validated conversion/evidence before integration, not an arbitrary nonempty string. Bind host/session monotonic clock and method in the session record; do not claim independent verification merely because caller supplied metadata. Mock evidence is explicitly mock. Tests: internally consistent rows from a wrong clock versus the expected session, conflicting alignment evidence, valid same-session clock, and missing/unverified binding. Raw unaligned telemetry may be saved as diagnostic evidence but must not produce measured session energy.

Also finish E2's postprocessing contract: current decoded-image timer ends immediately after callback. For now explicitly support CPU-synchronous postprocessing only and reject an async mode, or implement/test an explicit final synchronization hook for async adapters. Do not silently assume all future device callbacks are synchronous. No real runtime adapter is required in this repair.

### Parallel work and handoff

Luna1 fixes E3-E5 together, adds behavioral regression tests, updates protocol and records **L1A-004**. Tests must exercise the actual provider calls, time boundaries and expected-clock mismatch; a manifest containing the new fields alone is not validation. Push scoped branch changes including this inbox entry; no automatic merge, no changes to main graph worktree. Astra leaves the doc unstaged for Luna1.

Existing E2L1-001 permission for bounded **read-only raw telemetry availability samples** on E1/E2/E3/E5 remains in force, independently of this mock repair. If useful, Luna1 may collect a short bounded sample (at most 30 seconds per device, no workload generation or mode changes) with host monotonic receive timestamps, exact source/units and unavailable/permission states. Use existing tools/access only; no installation/sudo mutation. Such samples establish parser/source availability, not benchmark power, idle control, clock alignment accuracy or measured inference energy. No need to repeat unchanged inventory or wait for all devices. Preserve exclusive new output paths and do not commit private endpoints. Scored inference, TensorRT/Hailo conversion and benchmarking still need separate review.

## E2L1-005 — accept a1d62cf mock corrections; GO bounded raw telemetry feasibility

Reviewed commit `a1d62cf556434e197abf97da39e90aa618fc485d`, L1A-004. Astra independently ran `python -m unittest discover -s tests -p 'test_edge*.py' -v`: **23/23 PASS**; py_compile of harness/tests and git diff --check PASS. Worktree was clean before this entry. Review used API/interface-design boundary checks. No SSH, actual device inference, telemetry collection or benchmark was performed by Astra.

**Decision: ACCEPT E3/E4/E5 for the current CPU/mock harness.** Observed tests exercise non-256 pool permutation/wraparound and actual provider calls, measured-window energy excluding warmup (50J versus separately labeled 150J total-session fixture), expected-clock mismatch/conversion checks, and awaitable postprocess rejection. These complete the requested local repairs; do not open another generic mock-hardening cycle before collecting useful device evidence.

Acceptance is scoped: `completed_mock` remains mock-only. Callback return not being awaitable does not prove a future GPU callback has completed; a real adapter needs explicit stream/event synchronization. Injected fixture clock is not a real host clock measurement. A caller's `validated=true` conversion/evidence string is supplied provenance, not independent verification. Before a real energy benchmark, retain raw timestamps plus actual source/target clock identities, conversion parameters/evidence/uncertainty in persisted artifacts; the current integration result does not itself preserve the whole conversion mapping. These are real-adapter prerequisites, not blockers for raw telemetry sampling below.

### Next independent task: telemetry feasibility, not model benchmarking

Owner **Luna1**, same isolated edge worktree/branch workflow. GO existing authorized SSH aliases from `D:/Research/Teacher_Vu/docs/SHARED_INFRASTRUCTURE.md` for **E1/E2/E3/E5 only**. Main Luna still does not SSH server. No need to wait for YOLO26 graph preparation or for all devices to be reachable.

1. Reuse the accepted inventory/collector and obtain a short **raw read-only telemetry sample**, up to 30 seconds per device, using already installed tools and readable sensor files. Prioritize E2/E3; E1/E5 can proceed independently. No synthetic load, model load/forward, TensorRT/Hailo conversion, calibration, stress test or benchmark. No package installation, sudo mutation, fan/clock/power-mode changes, reboot or modification of another project's environment. Avoid repeated inventory of unchanged facts.
2. Use a bounded foreground child and capture raw stdout/stderr, exact command, return status, duration, device alias and source/code hash. Keep lifecycle scoped to the collector's own child; no global process-name stop command (including a global telemetry stop), no signals to other users' processes. If a tool's bounded execution/lifecycle is unclear, use simpler read-only sensor queries or record unavailable. SSH host-key checking remains enabled. Do not publish private endpoints/credentials.
3. Timestamp samples on the **device** with an identified monotonic clock where feasible; separately record local receive timestamps if used. SSH receipt time is not sensor acquisition time and must not be silently treated as aligned. Record buffering, sampling gaps, timestamp origin and unknowns. Raw power sources may have different rails/units and instantaneous/averaged fields: preserve original labels and units, do not sum overlapping rails or rename a module reading whole-device power. Missing power does not block thermal/memory collection; report unavailable.
4. Produce a small CPU parser/fixture validation for actual captured formats (if a new parser is necessary), preserving raw evidence alongside parsed values. Report observed memory, thermal/throttle fields and current power mode where available; do not call the device controlled-idle or steady-state from these snapshots. **Do not feed this run into measured inference energy/FPS/AP**: it has no model workload and establishes only source/parser feasibility.
5. Write exclusive new artifacts under `results/edge_readiness_v1/e2l1-005/<device_id>/<run_id>/`, with a concise feasibility summary: available channels, units/boundaries, clock limitations, privileges/tools missing, and which next adapter prerequisites remain. Preserve all earlier mock/inventory outputs. Artifact hashes must be computed from actual stored bytes; keep canonical Git hash handling explicit on Windows.

No external meter purchase/setup, Hailo compiler host change or scored device selection is implied. Unknown cooling/power-supply/external-meter information may remain unknown in this feasibility report; do not hold all progress for it. E1 CPU and Hailo remain possible conditions on one physical board, E5 is Orin Nano Super, and no legacy Nano/E4 substitution is authorized.

### Completion and gates

Luna1 may implement/test the small read-only capture/parser plumbing locally and execute this bounded task via existing edge SSH access under this authorization. If it would require installation or changing device state, stop only that affected operation and report the prerequisite. Append **L1A-005** with actual contacted aliases, exact action scope, artifact locations/hashes, tests and limitations; push scoped code/docs/artifacts including this entry using the established account. Do not merge another lane or stage unrelated files. Astra leaves this entry unstaged for Luna1 to commit/push.

GO raw telemetry feasibility now. NO-GO model inference, export/compile/build, benchmark, energy-per-image claims or matrix expansion. Those need source-model/runtime adapter correctness and a reviewed measurement protocol; mock acceptance alone cannot authorize them. Main Luna's G4/G5 work proceeds in parallel without a dependency on this task.

## E2L1-006 — accept telemetry feasibility; GO local Jetson adapter/smoke implementation

Reviewed 2026-09-17, commit `52e65bb6af8d3cf201a3bad64de2afd87b8e9f4f`, L1A-005. Astra independently reran **26/26 edge tests PASS**, checked **28 stored artifacts / 23 manifest hash comparisons** against canonical Git blobs, and re-parsed all four canonical stdout files: summaries exactly match the committed parsed reports. Four canonical transport exits are0 with five snapshot blocks each. E2 diagnostic attempt exit1 is preserved separately. No independent SSH or actual measurement by Astra.

**ACCEPT as raw telemetry/source feasibility, not benchmark readiness.** E1 has observed memory/thermal/throttle fields; E2/E3 memory/thermal; E5 also labelled power rails. No power channel found by these E1/E2/E3 probes means unavailable in this collection, not proof the hardware cannot expose power. E5 rails must remain separate with boundary unresolved. Samples are not a controlled-idle or inference-energy baseline.

E2 elapsed attempts are15.594s +15.515s = **31.109s**. Record as disclosed cumulative deviation from the <=30s/device task budget; each attempt remained bounded, and no performance result was generated. Retain both attempts; no discard/recollection needed. Future retries share the total device budget or require an explicit extension. The scope statement 'no signal' is too broad: `timeout` may terminate its own telemetry child, which was allowed; accurate claim is no signal/action targeting unrelated processes.

### Evidence qualifications for the handoff

- Device timestamps mark the start of a snapshot containing sequential sensor queries and a2s telemetry child. They are not individual rail-acquisition timestamps. Local receive start/end are transport interval endpoints, not per-line receipt times. Keep that distinction before any energy integration.
- Four canonical `remote_script_sha256` values match the current embedded script (`807c6bdb1c275643c0e0dc8ea66c2484d50deefc76b15ac4ca4bd0b873027d43`). Captured `collector_sha256` differs from the final collector file (parser was subsequently extended). Raw artifact hash verification therefore does not establish that the final complete Python file was the capture-time version. If exact earlier source is available, archive/hash it as supplemental provenance; otherwise state unavailable, never reconstruct it and call it original. This does not block using raw sensor text for parser feasibility.
- SSH exit0 is transport/outer script evidence, not all-source command success. Missing/error source lines remain present. Local SSH timeout alone also does not prove every remote child terminated. Before future repeated/long-lived captures, implement bounded remote lifecycle/source statuses; do not use global `tegrastats --stop` or kill-by-name.

### Next useful work, independent of graph lane

Owner Luna1: **GO local implementation/tests and a concrete proposed smoke protocol**, no device execution in this new phase. Build one coherent package for a Jetson GPU-native adapter, starting with **E2 Xavier NX as proposed first smoke target**; E3/E5 are subsequent compatibility candidates, not automatic scored scope. This is feasibility planning, not choosing a winning arm or final paper device list.

1. Reuse existing TensorRT/inference helpers where possible. Implement a narrow backend adapter and CPU mocks for engine/runtime/device bindings, batch1/640 I/O validation, dtype/buffer handling and explicit synchronization; describe handling of E2/E3 TensorRT8.5 versus E5 TensorRT10.3 from their observed inventory. Use official vendor documentation for version-specific API claims, cite sources. Do not import/use actual GPU runtime locally, install packages, rebuild engines or transfer server engines to edge.
2. Prepare a **proposed** correctness smoke using frozen YOLO11n FP16 as an engineering reference (not arm selection). Identify the accepted checkpoint/hash from repo evidence, proposed device-native conversion path and required graph/runtime inputs, exact preprocessing/postprocessing and a small deterministic train-only image fixture shared with a source reference. Bind actual image bytes/order. Propose output checks and numerical tolerances before observing device outputs, with justification; do not compute AP or use official-test images. Keep native output contracts and avoid double NMS. Do not execute export/build/forward yet, locally or on device.
3. Integrate the accepted harness only as a mock contract now: inference-only versus end-to-end timer, explicit GPU stream completion including asynchronous copies/postprocess, warmup exclusion, source-weight versus engine hashes, session/image sequence and actual runtime/device identity. No claim that a host enqueue timer is completed-device latency. Keep mock artifacts clearly segregated.
4. For future real telemetry sessions, design per-source timestamps/clock conversion evidence, remote collector lifecycle and source exit/error handling. Persist raw source text and conversion provenance; absent alignment/power boundary means energy unavailable rather than blocking latency/correctness. Add captured-format fixture tests from accepted telemetry and mocked timeout cleanup tests. Do not repeat device inventory now.
5. Produce one review package: code/tests, proposed smoke command(s), explicit allowed resources/mutations, disk/dependency checks using known inventory, expected artifact layout, and stop conditions. Missing external meter/cooling data is a declared limitation to resolve before scored power measurements, not a reason to stall local adapter work. Hailo compiler planning can remain documented but no Hailo conversion/API expansion in this package.

Record **L1A-006**, including the above telemetry qualifications, exact tests and remaining prerequisites, and push scoped edge branch files including this entry. Astra reviews the complete adapter/smoke package before authorizing any model transfer, device-native build or inference. No PR merge or modifications to the main graph lane. No need to wait for v26 Mod metadata repair to implement this local package. Leave previous artifacts immutable; Astra leaves this instruction unstaged for Luna1 commit/push.

## E2L1-007 — adapter review: correct buffer ownership and finish the executable mock contract

Reviewed 2026-09-17: `e728b51e95ffaa722679fcff928e664f1e75a25b`, L1A-006. Astra independently reran **34/34 edge tests PASS**, without SSH, CUDA/TensorRT imports, engine build or inference. **CHANGES REQUESTED, GO local repair only.** Accepted telemetry feasibility remains accepted; do not recollect it. Main Luna's numeric repairs proceed independently.

### J1 — injected buffer addresses must actually own copies and enqueue

`JetsonRuntimeAdapter.__init__` constructs `_device_bindings` with `device_pointer=index+1`, independently of the injected buffer manager. The current mock also uses 1/2, masking this bug. Astra replaced only mock-manager addresses with `[4096,8192]`: the E5 adapter still called `set_tensor_address(images,1)` and `set_tensor_address(output0,2)`. On E2 the enqueue receives manager addresses but copy operations receive the separately invented descriptors. This is not a cosmetic mock issue: the adapter abstraction must bind the same storage throughout.

Have the buffer owner supply validated allocation descriptors/pointers, lifetime and host staging buffers. Never generate device addresses in the runtime adapter. Use the same binding map for H2D, v2 pointer-list order, v3 named addresses and D2H. Make copy-stream ownership explicit (pass the stream or require and validate a shared-stream contract); synchronizing an unrelated stream is not copy completion. Return actual host output payloads/descriptors after the final completion boundary, not only `DeviceTensor` metadata. Mock payloads are fine in this phase.

Add tests with nontrivial/reordered addresses for BOTH APIs, copy-address versus enqueue-address equality, host output availability only after completion, failed named-address binding, failed copy/enqueue, missing/duplicate pointers and unsupported binding roles/locations. Reject unsupported host-location bindings rather than treating them as device copies. Keep actual GPU runtime imports/execution outside local tests. This repair does not request a CUDA allocator or a device run now; the injected contract must be coherent first.

### J2 — integrate with the existing harness and freeze a meaningful smoke fixture

The current tests exercise adapter and harness separately. Add an actual CPU/mock integration using this adapter through the accepted harness, with an explicit bridge for its input/output types, backend/synchronization metadata and CPU-synchronous postprocess. Verify the two declared timing boundaries, final-copy completion, warmup exclusion, image consumption and structured failure propagation. Small test sessions are sufficient; do not call them latency measurements.

The three generated 4x4 PPMs are SYNTHETIC mock fixtures, not CCTSDB training images. Rename/classify them accurately and retain them for unit tests. For the proposed real smoke, bind three deterministic accepted CCTSDB train images (first three U42 IDs in canonical order, with accepted source content hashes); use existing local data/readiness evidence if available, otherwise record the exact missing materialization prerequisite without claiming synthetic data is train evidence. Do not read official test or negative-test images. Same image bytes and derived input tensor must feed source and target references.

Freeze the YOLO11n three-class native raw output contract `[1,7,8400]` for the proposed no-NMS engineering engine, with source-to-engine naming/mapping explicit. Current tests use `[1,84,8400]`, which is generic 80-class geometry and cannot validate this checkpoint's output. Either keep a generic adapter plus a separate mandatory model-contract validator, or bind this scoped adapter directly; reject same-positive-shape-but-wrong-model outputs before comparison.

Numerical tolerances must describe compute/reference mode and box-versus-score domains, not be chosen solely from output buffer dtype: a proposed FP16 engine can expose FP32 I/O. Keep thresholds fixed before device observations and record the comparison equation. This is a proposed smoke threshold, not a demonstrated precision bound. Reference outputs need an explicit tensor name/coordinate mapping; PyTorch output names are not intrinsically TensorRT binding names.

### J3 — complete the local smoke workflow proposal without premature device execution

E2L1-006 requested a concrete review package. The documented command currently points to nonexistent `jetson_adapter_smoke.py` and is explicitly a placeholder. Provide an executable CPU/mock/dry-run orchestration path that validates the planned inputs, uses the adapter/harness boundaries and emits the proposed manifest/layout, including failure artifacts. Describe the future real target entry point and dependencies honestly; do not label a mock command as a working device workflow. Target-native conversion/export recipe, source-reference location, required files, dependency/disk checks and exact allowed writes must be specified so a later authorization can be bounded. No package installation, target model transfer, export/build, actual forward, SSH or scored benchmark in this repair.

Implement/test the already requested collector lifecycle and per-source error/clock records through mocks/captured telemetry fixtures, or list those pieces as genuinely unimplemented prerequisites. Crucially, remove missing power/alignment from unconditional correctness-smoke stop conditions. Missing power or clock alignment makes energy **unavailable**; it does not block model correctness or correctly synchronized latency. Missing final inference synchronization, wrong model/binding or runtime incompatibility DOES block correctness. No fabricated measured energy, no rail summing or automatic device-state changes.

### Handoff and exit gate

Address J1-J3 in one coherent local package and append **L1A-007** with implementation commit, exact tests, mock-versus-real boundaries and remaining prerequisites. Push scoped branch files including this entry unchanged using the established account; do not merge lanes or stage unrelated files. Astra leaves this handoff unstaged for Luna1 to commit/push. Use official version-appropriate Python API references for any newly introduced actual-runtime claim.

GO CPU/mock implementation now, independent of the main numeric lane. NO-GO SSH/model transfer/device-native export/build/inference/benchmark at this gate. E2 remains proposed first correctness-smoke device, not a paper performance winner. No new inventory loop and no waiting for main Luna to finish before these fixes.

## E2L1-008 — accept repaired CPU/mock contract; prepare the real E2 runtime path

Review 2026-09-17 of `2e546ef1bddea5b4b5c2f40274dad5f5781d5c39`, L1A-007. Astra independently ran **39/39 edge tests PASS**, including CLI dry-run/failure paths and both harness boundaries, plus py_compile and git diff --check. Reviewed owner-provided addresses/copy stream, v2/v3 dispatch, final host payloads, synthetic fixture classification and native three-class output validation. No SSH/device runtime/model execution by Astra. **ACCEPT as CPU/mock contract evidence**, not a working real TensorRT adapter or an executed correctness smoke. No need to repeat accepted inventory/telemetry or the same mock-only milestone.

### Next useful package, independent of main numeric work

Owner Luna1. GO local implementation of the concrete real-runtime bridge, source/fixture preparation code, comparison code and an executable proposed E2 correctness-smoke workflow; test locally through injected CPU doubles. This does not yet authorize actual conversion/build/model forward, CUDA context allocation, model transfer, scored timing or power measurement. Reuse E2 TensorRT8.5.2.2/Python3.8.10 evidence; do not require the desktop/local Python version on the Jetson or install packages by assumption. E3/E5 remain candidates, not automatic new device runs.

1. Implement a lazy device-runtime provider for the approved existing E2 software, with explicit allocation/free lifetime, engine deserialization/binding introspection, stream and copy ownership, synchronized host outputs and cleanup on partial failure. No GPU libraries loaded/executed during local tests. Keep the mock-only bridge/backend identity separate from real mode. Validate returned host outputs against the engine's expected name, shape, dtype AND nbytes, not merely `len(payload)==tensor.nbytes`: the current adapter would accept a self-consistent wrong-sized HostTensor. Test repeated-call freshness, output descriptor mismatch and failure cleanup. Likewise guard the whole new output root so rerunning a completed dry-run cannot append a new failure marker to old success evidence.
2. Bind actual fixture/source inputs now rather than report them permanently missing. Astra verified local presence of frozen YOLO11n checkpoint and train images `00006.jpg`, `00009.jpg`, `00028.jpg` under `D:/Research/paper` (read-only observation, not yet hash approval). Main graph worktree contains accepted readiness/U42 content bindings. Read those canonical metadata/bytes without modifying either other worktree, verify against accepted hashes and write an edge-owned manifest/optional private fixture copy. Do not commit dataset images, model bytes or large tensors. No source-model forward yet. Implement the scoped source-reference/preprocessing helper and its tests; identify exactly where a later source reference will run and which versions are needed. PyTorch source checkpoint provenance is distinct from a server TensorRT engine: the RTX engine must not be transferred to E2.
3. Replace the remaining vague conversion recipe with explicit compatibility decisions: accepted source checkpoint, ONNX source/export settings and destination, opset/operator compatibility to be tested on TRT8.5, target-only FP16 build settings, required Python/CUDA bindings and all expected artifacts. An ONNX may be produced elsewhere only through a separately authorized source-export step; do not silently assume current Ultralytics/Torch can run on E2 Python3.8 or that a TRT10 ONNX workflow has been validated on TRT8.5. No real export/build now. A missing dependency is a prerequisite to report, not permission to install or alter JetPack.
4. Finish the proposed numeric comparator before seeing outputs. The protocol still selects tolerances from "FP16 raw output" versus "FP32 raw output"; that is not the requested compute-mode versus I/O distinction. A target FP16-compute engine with float32 buffers is NOT a float32-compute reference. Define the source reference, intended compute mode, binding dtype and separate box/score tolerance domains, equation/reference magnitude, finite handling and mismatch summaries. `output0 [1,7,8400]` is the head's decoded box coordinates plus scores before NMS, not convolution logits; specify coordinate convention/units and class order from pinned source evidence. No test-set tuning, post-hoc widening, AP, arm selection or latency claim. These are engineering diagnostics, not a theoretical error bound.
5. Provide one executable staged workflow (preflight / source-artifact checks / target build / inference compare) with side effects explicit and disabled unless separately authorized. Tests may mock stages; actual model export/build/forward remains NO-GO until Astra reviews this complete package. A correctness smoke need not run the 200/1000x3 timing study or telemetry collector; keep power unavailable where alignment/boundary is missing. Future scored sessions need the already disclosed collector lifecycle/clock handling, but those do not block this local correctness implementation. No new inventory-only cycle is requested.

### Narrow read-only E2 capability probe, only if needed to choose the runtime provider

If existing inventory lacks the decisive API/package information, Luna1 is authorized to use the established **E2 alias only** from `D:/Research/Teacher_Vu/docs/SHARED_INFRASTRUCTURE.md` for one bounded read-only dependency/API probe (up to60 seconds cumulative remote command time, including retry). Use existing Python, metadata/import-spec discovery and module/class introspection as needed: Python/NumPy/TensorRT versions, installed CUDA Python binding availability, relevant API attributes, disk capacity and existing scoped path permissions. No `pycuda.autoinit`, CUDA context/allocation, engine deserialize/parser/build, model load, forward or benchmarks; no package installation, sudo, chmod, clocks/fans/reboot, environment dumps, credential printing, other-user process actions or writes to device data. Import-time bytecode writes should be disabled. If a binding's import may initialize CUDA, restrict to metadata/source discovery instead.

Preserve host-key checking and bound the remote child, not only local SSH waiting. Capture sanitized command, source hash, stdout/stderr, exit status and elapsed time to a new local edge artifact root `results/edge_readiness_v1/e2l1-008/`; no private endpoint credentials in Git. Missing APIs/packages should end that probe, not trigger repeated troubleshooting on device. No need to wait for a free server/GPU for this metadata-only check. Do not probe E1/E3/E5 in this task.

### Handoff

Append **L1A-008** with real versus mocked implementation coverage, actual dependency evidence (or accepted evidence reused), verified fixture hashes, source/export/target runtime contracts, exact candidate commands, all expected writes and tests. Include a concise remaining-prerequisite list; no claim that tests alone establish real correctness. Push scoped branch code/docs and this entry unchanged; leave other worktrees and historical results intact. Astra leaves handoff unstaged for Luna1 to push. Main Luna follows A2L-033's separate conditional CPU-run gate; the two lanes continue independently.

## E2L1-009 — accept dependency/fixture findings; repair asynchronous runtime boundary

Review 2026-09-17 of `0f2dd23b83e987c2ff51560990fc7b3d9558c2be` / L1A-008. Astra independently ran **51/51 edge tests PASS**. Actual local source JPEG verification reproduces fixture sequence hash `7bfaaa99c4ed6b8ea2c92695f68747496a2ebb24400cf978b8630c27f41a6fc9`; current probe source reproduces recorded script hash `5b84148b10a383d55fc6252af4b6599a0c2ec004f417aa619a6747c843f8641d`. No SSH, CUDA/TensorRT import, model forward or build by Astra. **ACCEPT observed dependency/fixture feasibility, not end-to-end runtime readiness.** Do not repeat the probe merely to obtain the same inventory.

The reported environment lacks discoverable PyCUDA/cuda-python, Torch/Ultralytics/ONNX/ORT in the probed E2 Python environment. That does not prove no other environment exists, but it means this provider still has NO concrete validated CUDA memory/stream implementation. `CudaMemoryOwner` is an injected interface, not an installed provider. No install/change to JetPack or another environment is authorized. The metadata probe's source-token search is not actual TensorRT API validation; missing tokens in an __init__.py wrapper do not mean compiled APIs are absent.

### R1 — D2H payload is frozen before asynchronous copy completion

`OwnedBuffers.copy_device_to_host()` allocates a bytearray, enqueues its copy and immediately calls `bytes(destination)`. Only later does the adapter synchronize. Thus a genuinely asynchronous owner can populate the destination AFTER the returned immutable bytes have been copied from stale contents. Astra reproduced this entirely with a CPU deferred-copy double: after completion the destination's first float is **42.0**, while `output_tensors()['output0'].payload` is **0.0**. Existing FakeMemory writes synchronously and misses the bug.

Retain the actual mutable host staging allocation until stream completion, then materialize immutable host bytes in/after the completed readiness boundary. Define/reset per-call pending/output state at the start of each inference, before H2D; no previous-call readiness or stale host payload may survive a failed second call. Reject calls after buffers are freed; keep cleanup idempotent and release every allocation even after another cleanup error. Add a delayed-copy fake that only fills output during synchronize, nonzero/different payloads on successive calls, second-call H2D/enqueue/D2H failure, output access before sync and after free. Test through provider + real OwnedBuffers + adapter together, not just isolated mock buffers. This is a correctness blocker for device execution, independent of performance measurement.

### R2 — real evidence must come from runtime/bindings, not caller declarations

`TensorRTProvider._describe_engine` currently labels every binding `device` and builds the descriptor's runtime version from `config.runtime_version`, not the imported runtime. Query/check observed runtime version before deserializing; query actual binding/tensor locations and reject unsupported host/shape/dynamic I/O. Retain required logger/runtime/engine/context lifetimes explicitly, detect null execution context and test partial construction failures. Invoke the mandatory YOLO11n native output contract before the scoped provider can create an inference adapter. Keep E2 TRT8.5 as the scoped real candidate; do not claim the untested TRT10 branch is device-validated.

Continue to describe source/native output as **decoded xywh in pixels of the 640x640 letterboxed input, before NMS**, not "normalized-to-640" or pre-box-decode logits. Comparator compute/I/O separation and fixed box/score equation are accepted as a proposed engineering diagnostic, not a mathematical precision bound. Fix JSON diagnostics for nonfinite inputs: current `.as_dict()` exposes Infinity/NaN, so strict `json.dumps(..., allow_nan=False)` fails precisely on an error case. Persist null plus explicit nonfinite counters/reasons; validate finite nonnegative policy tolerances. Add nonfinite/reference-zero tests without widening thresholds.

### R3 — complete error artifacts and disclose feasibility limits

`e2_correctness_workflow.main` catches an exception after `run_stage` has created its output directory, then calls `mkdir(exist_ok=False)` on that same directory, masking the original error and preventing `failure.json`. Fix owned-new-root versus pre-existing-root handling: create exactly once, record failure only inside the root created for this invocation, and never mutate a prior run. Test missing fixture after root creation and a second invocation on both successful and partial roots. The current preflight only RECORDS the deployment config hash, rather than checking an accepted hash/schema; call it observed until that binding is enforced. "source-artifacts" currently verifies image files only, not source model outputs/ONNX, and must remain labelled accordingly.

For the next handoff, close the implementation feasibility question: specify a concrete CUDA-owner option using software already present, with allocator/stream/copy/free API and dependencies, OR explicitly request a bounded dependency-install decision if no such option is practical. Do not replace a missing implementation with another placeholder called a completed real provider. Local implementation through injection/tests is GO; actual CUDA calls remain NO-GO. A native runtime option may be proposed, but ABI bindings must be tied to installed CUDA headers/vendor documentation rather than guessed. Source export/reference is still a separately scoped future action; do not silently install Torch on E2. No new device probe in this task.

### Probe qualification and handoff

The retained probe ended successfully in recorded 1.047s. Its code only bounds local SSH waiting, not the remote child, despite E2L1-008 requesting both. Disclose that limitation; do not retrospectively claim a remote timeout guard or rerun the successful metadata capture. Before any future remote operation, add/test a remote bound for its own child, plus target-alias validation. Do not kill unrelated processes or use global stop commands. Finding `cuda.cudart` with `find_spec` can import its parent when present; keep no-CUDA-import claims scoped to what this actual probe observed, and use metadata/top-level discovery where future import side effects are unknown.

GO Luna1 local R1-R3 repair/completion, CPU deferred-operation tests and protocol correction; append **L1A-009**, push scoped code/docs and this entry unchanged. Keep existing evidence immutable. NO-GO device transfer, CUDA allocation, engine deserialize/build, source/target model forward, timing or energy benchmark. This is not a GPU-capacity restriction: a real asynchronous output can currently be wrong even on an idle device. Main Luna has independent A2L-034 CPU-run GO and does not wait for this lane. Astra leaves this entry unstaged for Luna1 push.

## E2L1-010 — accept R1-R3; implement a concrete CUDA-owner candidate, not another mock-only completion

Reviewed 2026-09-17: `0630b0b786f156d431dd865c9147f16b10876722` / L1A-009. Astra reran **58/58 edge tests PASS**, py_compile and git diff --check. The deferred-copy provider/owner/adapter regression now delivers 42 then43, previous output is reset before a second call, and freed-buffer reuse is rejected. Runtime/location checks, strict-JSON diagnostics and owned output-root failure handling are covered. **ACCEPT these R1-R3 repairs as local contract evidence.** No real CUDA/model execution has been established. Do not repeat the same inventory, fixtures or mock-only milestone as a substitute for implementation progress.

The remaining practical blocker is specific: `CudaMemoryOwner` and stream are still interfaces/doubles, with no installed PyCUDA/cuda-python in the probed E2 Python environment. No dependency installation was authorized or performed. A concrete memory/stream owner must exist before this workflow can run on E2. Missing power telemetry is NOT this blocker.

### Next deliverable: complete one real-owner implementation plus proposed allocation-only smoke

GO Luna1 local implementation/review package for **one** E2-native owner route. First evaluate the already installed CUDA11.4 runtime/library route (for example a narrowly scoped Python ctypes binding to its documented C runtime API, with a lazy injected library loader). Do not guess ABI signatures or claim that a CUDA version string proves the required library/headers exist. Use official CUDA11.4 API/header evidence for pointer/size types, stream/copy/memory functions and error handling. Do not expand to TRT10/E5/Hailo in this task.

If this route is unsuitable, report a concrete alternative and the exact minimal install/environment permission needed instead of quietly installing or indefinitely restating "provider missing". This task does NOT authorize installation, compilation on device, changing system packages, sudo, modifying shared environments, clocks/power/fans or another process. No unreviewed CUDA calls locally or on E2.

1. Implement the actual lazily loaded owner/stream class behind the accepted interface: allocation/free, H2D/D2H and completion, context/device ownership assumptions, raw return-code checks and scoped cleanup. Either synchronous copies are explicitly declared and correctly bounded, or asynchronous copies retain valid host memory (pinned staging where required) until completion; ordinary Python bytearrays must not be silently assumed CUDA-pinned. Retain source/destination lifetimes. Keep tests through injected deferred C-function doubles and the provider integration, not by actually loading a GPU library locally.
2. Include cleanup/lifetime refinements in this integration: wait for this stream's pending transfers before releasing staging/device allocations on error; keep the TensorRT logger alive until its runtime is destroyed (current `close()` drops logger before runtime); preserve the observed runtime version in the descriptor rather than relabelling it solely from config. Test failures at allocation/copy/sync/free and repeated use/close without acting on other processes. Do not redesign the study or add benchmarking here.
3. Implement a **proposed, default-disabled allocation-only smoke** with exact resource limits: E2 only, one owned stream, a small deterministic host payload and at most1MiB device allocation, one H2D/D2H roundtrip, exact-byte comparison, synchronization and cleanup, no engine/model/TensorRT inference. The command and side effects need to be explicit so Astra can authorize a useful real device test next. CPU tests exercise it with doubles now. This is NOT authorized to execute on device under this entry.
4. Package the source-reference/export plan in the same handoff so later work can proceed without another placeholder round: existing frozen YOLO11n, three verified train images, input tensor recipe/reference output shape, source environment and intended source-run location, static ONNX settings compatible candidate for TRT8.5, exact artifact/hash roles. Any source runner may be implemented and tested with CPU doubles now; actual frozen source forward/export remains a separate scoped authorization. The current server numeric v8/v26 run is unrelated and must not be repurposed as YOLO11n reference evidence.

### Narrow new evidence allowed only to resolve native-owner feasibility

Existing probe establishes package absence but not CUDA C-library/header availability. If needed, GO one **read-only E2-only SSH inspection** through the existing authorized alias for this NEW information: library/header realpaths/version and required symbol/header declarations, permissions and loader discoverability. Use bounded filesystem reads / installed metadata or symbol-inspection tools, never `ctypes.CDLL`, CUDA initialization, context/allocation, engine deserialization or model operations. No broad environment/secret dump. Remote child bound<=50s, local timeout greater than that, cumulative remote inspection<=60s including retries; preserve sanitized commands/raw stdout/stderr/status/source hash under a new local `results/edge_readiness_v1/e2l1-010/` root. If required evidence is absent or inaccessible, report it rather than install or repeat indefinitely. No inventory/telemetry rerun on other devices.

### Exit condition and handoff

Append **L1A-010** with chosen concrete owner/dependency route and evidence, implemented versus mocked functionality, tests, and exact candidate allocation-only/source commands with side-effect limits. Push scoped code/docs/any new read-only evidence and this entry unchanged; no automatic device invocation when tests pass. Astra reviews the ready implementation to authorize the small memory roundtrip, then model smoke separately. This local work and optional metadata-only probe run independently of main Luna's A2L-035 repair/operator CPU run. Astra leaves handoff unstaged for Luna1 push.

## E2L1-011 — fix the executable smoke lifecycle; conditional GO one E2 allocation-only roundtrip

Review 2026-09-17: `7d6754c` / L1A-010 in `D:/Research/paper-luna-edge-002`. Astra reviewed the concrete CUDA runtime/owner and allocation smoke and reran **63/63 edge tests PASS** (including runtime tests) with CPU doubles. No real CUDA, SSH, model forward or benchmark was performed by Astra. ACCEPT the concrete lazy runtime implementation as a candidate, not as device-validated. Main Luna numeric localization is independent.

### C1 — repair Python3.8 artifact writing and complete lifecycle evidence

The observed E2 interpreter is Python3.8.10. Both success/failure paths currently call `Path.write_text(..., newline=...)`, whose newline argument was added only in Python3.10: [official Python documentation](https://docs.python.org/3.10/library/pathlib.html#pathlib.Path.write_text). This can fail AFTER device work and then fail again while recording the exception. Use a Python3.8-compatible exclusive writer (for example `open(path, 'x', encoding='utf-8', newline='\n')`) and strict JSON. Audit this smoke's imported execution path for the same compatibility issue; do not claim a current-interpreter mock test proves execution on3.8.

Move runtime load/open, stream creation and owner construction inside the owned-root error lifecycle. On every failure preserve the primary error plus all cleanup errors, and clean up only resources actually created. Currently initialization failures bypass failure.json, and manifest PASS is written before stream/runtime close can fail. Emit terminal PASS only after required synchronization/free/stream destruction/runtime close succeed. A failed or incomplete cleanup must remain visible and produce nonzero exit, without replacing the primary exception. The existing root must be refused before CUDA library load and remain unchanged.

Test load failure, stream-create failure, allocation/copy/sync/free/destroy/close failure, simultaneous primary+cleanup failures, successful roundtrip followed by cleanup failure, and pre-existing success/partial roots. Test the complete CLI-to-writer path with injected C doubles and a Python3.8-compatible writer signature, not just `run_roundtrip`. Preserve raw CUDA return codes and operation names.

### C2 — allocation generations and honest context/resource wording

`CudaRuntimeMemoryOwner._freed` permanently remembers pointer integers, so a later allocation reusing an address can be skipped by free. Track live owned allocations/generations (and reject unknown/freed pointers for copy/free as appropriate), allowing a newly allocated reused address to be freed again. Retain safe idempotent cleanup within an allocation's lifetime. Add a fake allocator returning the same address across allocate/free/allocate/free and assert two native frees; do not call a freed pointer live because it once belonged to this owner.

Clarify that CUDA runtime calls can initialize/use the process's device context. This smoke is NOT a no-CUDA-initialization or no-context-allocation operation. The4096-byte payload/1MiB cap bounds explicitly requested data allocations, not total CUDA context/driver memory. It creates no model, TensorRT engine or workload in another process and must not reset a device or change clocks/configuration. Correct the report's proposed "server-side D:\Research\paper" location: that is a Windows local path, not the Linux server path. Source-reference/export is future work, not authorized here.

### Conditional device GO, without an extra report-only approval round

GO local C1/C2 repairs/tests and protocol now. Once these regression tests and existing edge tests pass, the committed smoke remains exactly the allocation-only contract below, and the repaired code is pushed and byte-bound, **GO Luna1 ONE real E2-only allocation smoke**. If repairs require a different API/dependency/resource operation or tests fail, return for review instead. No automatic retry after an attempted CUDA execution, and no inference authorization follows a successful copy.

- Use the already approved E2 SSH alias from shared infrastructure; verify its current hostname/architecture/Python3.8/runtime-library identity against the accepted E2 probe before CUDA dispatch. A `--target E2` string alone is not target verification. Stop on unexpected target/runtime; do not install or substitute dependencies. Record actual library realpath/version and code hashes.
- Transfer only this scoped committed Python source/dependencies into a NEW user-owned, versioned E2 directory (resolve and report its absolute path; do not overwrite a shared checkout). No model, ONNX, engine, image or payload file transfer is needed. Verify transferred code hashes and record the exact pushed commit and sanitized command. Choose one absent output root ending in `e2l1-011-allocation-v1`; local evidence root is `results/edge_readiness_v1/e2l1-011/` and must also be absent.
- Execute `e2_cuda_allocation_smoke.py --target E2 --execute-real-device --out-dir <verified-new-absolute-root>` once, using observed system Python3.8.10 and installed libcudart11.4. Only one stream, one4096-byte deterministic device buffer, one synchronous H2D and D2H, completion, exact-byte comparison and scoped cleanup. Enforce the1MiB allocation ceiling before allocating. No CUDA DeviceReset, TensorRT import/deserialization, build, model forward, timing benchmark, telemetry sampling, package installation, sudo or configuration/process modification.
- Bound this smoke's own remote child to60s and local wait to75s; do not kill other processes. Record timeout/partial evidence rather than rerun. A deadline termination cannot be labelled confirmed clean teardown. Do not require globally idle GPU for this tiny copy-correctness smoke: record resource errors normally; no performance/isolated-GPU claim is being made.
- Collect source/transfer hashes, sanitized stdout/stderr, exit/timeout status and complete manifest or failure artifact including cleanup status. Success requires exact4096-byte hashes AND completed cleanup. If transport fails after dispatch, execution state is unknown until read-only recovery checks; never repeat blindly. Preserve remote evidence and only publish JSON/logs, no device binaries.

Append **L1A-011** with implementation commit, test evidence and actual device result (or precise unmet conditional gate). Push scoped code, this inbox entry unchanged and sanitized artifacts using NADUNGVN. Stop after the allocation result. NO-GO source/model forward/export, engine build/deserialization/inference and benchmark remains in force; the next meaningful gate is a separately reviewed model smoke, not another repetition of the same mock milestone. Astra leaves this entry unstaged for Luna1 to commit/push.

## E2L1-012 — accept real allocation smoke; prepare one frozen YOLO11n source/reference bundle

Review 2026-09-17: `ec232dd` / L1A-011, executed code `aa31c42825fa9995a6a8d1048a87d9c4fb8385f6`. Astra checked the four canonical evidence files: manifest/stdout SHA256 `e2c29422dc49af47cbeacad8e727b955764cc0bb33157db423a7dac829dde823`, dispatch `df8c59a7309f5bcb2db581ba6aca414c55d24e90bc9e36b24df843558b333599`, empty stderr. All three transferred source hashes match canonical code blobs at the executed commit. Dispatch records E2 identity, one exit0/no-timeout execution, exact4096-byte payload hash and successful stream/runtime cleanup. Astra independently reran **68/68 edge CPU tests PASS**. No real device action was taken by Astra.

**ACCEPT allocation-only copy correctness on E2. Do not repeat allocation/probe/telemetry.** This does not prove TensorRT bindings/enqueue, model output, performance, energy or isolation. Next deliverable is the source model/reference bundle required for an actual E2 model smoke. Main Luna's v8/v26 diagnostic is independent; do not wait for it and do not reuse its outputs as YOLO11n reference.

### P1 — implement the source bundle producer and tests locally

Implement one executable CPU-only source-preparation stage; current `source-artifacts` verifies JPEGs only and is not a materialized model/reference stage. Keep its old result meaning intact or add a clearly distinct stage. Required inputs:

- Frozen YOLO11n `results/yolo11n_cctsdb_clean_s42_v2/weights/best.pt`, SHA256 `3e5fc7a2148c16539cd9fb7cc7cacd81a4eec1dfc28143cdf9b6dcd872ba4ab8`.
- Existing accepted train fixture00006/00009/00028 in that order, with all existing JPEG byte/hash checks. No dev/test/negative-set evaluation.
- Batch1 input `[1,3,640,640]`, CPU float32; decoded pre-NMS output `[1,7,8400]`, xywh in letterboxed-input pixels plus three class probabilities. Inspect actual native head/flags and class order; do not treat raw debug box parameters/logits as this output.

Use actual installed Ultralytics image-decoding/LetterBox semantics and record parameters, RGB conversion, normalization, contiguous NCHW layout, array dtype/endian and SHA256. Freeze each input tensor once, and use those IDENTICAL bytes for native/ONNX/future E2, eliminating preprocessing drift from the first model smoke. This is input-tensor inference correctness, not a complete camera/preprocess/end-to-end deployment claim. Include bounded tests comparing the fixture helper to the actual producer on synthetic images; do not assume helper names prove equivalence.

Create the export from a private COPY of the frozen checkpoint in the owned output root so exporter side effects cannot overwrite accepted best.onnx/best.engine/weights. Freeze options: CPU, float32 ONNX, opset17, static batch1/640, no NMS/end2end wrapper, `simplify=true`, no Q/DQ/calibration/TensorRT. Validate exported I/O/shape/dtype/names and run ONNX checker. No automatic package download/install or fallback to different export options. Record exporter/runtime/source hashes and observed model flags/fusion; preserve an independent ordinary native FP32 reference object before exporter modifications. Export-internal tracing/dry runs must be counted/reported separately from fixture inference; one exporter invocation is NOT one model forward.

Plan one ordinary native forward per fixture and one ORT CPU-only forward per fixture (3+3), plus the single exporter invocation's actual internal calls. Save private reference/output/input tensors with byte hashes, dimensions and finite checks. Compare native vs ONNX anchor-aligned decoded box and score channels using a predeclared source-equivalence diagnostic equation `abs(observed-reference) <= 1e-5 + 1e-4*abs(reference)`; record counts, maxima and bounded failing elements. Do not use the existing looser FP16 E2 tolerance for this FP32 source comparison or relax a failure. Current target FP16 tolerance remains unchanged for its later, distinct purpose. FAIL is a reportable outcome, not permission for re-export/tuning/fusion/reference changes.

New output: `results/edge_readiness_v1/e2l1-012-source-v1/`, absent at start. Public plan/manifest/report/logs/index; private ONNX, copied checkpoint and input/reference tensors remain outside Git under the owned private subdirectory with explicit retention paths/hashes. Write failures and attempted/completed counters on every stage. Never overwrite previous artifacts or re-use an existing partial root. Protect original checkpoint/JPEG hashes before/after. No engine or calibration/timing cache.

### P2 — bounded execution gate and environment routing

GO P1 local implementation, actual-producer synthetic CPU tests and end-to-end injected-runtime tests now. Cover schema/shape/semantic mismatches, exporter mutating its private copy only, package absence/no auto-install, strict CPU provider, output collisions, partial failures and private/public inventory separation. Pin/report a complete source environment BEFORE any real model/export dispatch.

Preferred real source execution is local, if an existing complete environment is available, with Ultralytics8.4.102 and all required Torch/ONNX/onnxslim/ORT/image libraries. Astra's read-only metadata check found the existing `D:/Research/paper/local/measurement_audit_env` lacks the `onnx` distribution; do not launch export there assuming readiness or install into it automatically. You may inspect existing local environments without changes. CPU local source results must be labelled with their ACTUAL versions (not server Torch2.5.1 if different) and treated as this edge engineering-smoke reference, not a reproduction of the server study.

If local requirements are absent, use the existing complete server source environment via the USER: main server repo `/home/ubuntu/Dung_TDTU/nighttime-tsd-new`, interpreter `local/g0_size_env/bin/python`, user pull/runs/pushes as usual. Luna1 must not SSH SERVER-01 or silently execute on E2 for source export. Supply branch-scoped integration instructions that preserve the main Luna worktree; no forced checkout/reset or unreviewed merge of unrelated branches. A separate worktree or reviewed scoped integration is acceptable, with exact code/input paths bound in the manifest. Do not consume the other lane's output root or overwrite its inbox.

After P1 tests pass and an existing complete environment has been verified, **conditional GO ONE CPU source-bundle run** with the scope above, either local by Luna1 or server by the user. Freeze all observed versions, code commit, input hashes and options in the plan before invoking the exporter. CUDA hidden, CPU threads limited to2, ORT CPU provider explicitly required, downloads/auto-install disabled. Foreground only; no GPU-idle guard needed. If a new dependency, export design change or runtime incompatibility is required, stop with the exact request instead of improvising. No retry or duplicate source run on a second environment.

Append **L1A-012** with implementation/tests, chosen environment and exact commit, plus actual source outcome or the operator command if awaiting server execution. Push scoped code/docs and this unchanged inbox using NADUNGVN; publish only JSON/text evidence, not ONNX/weights/tensors. Report numerical FAIL/unresolved honestly and stop for Astra review. **NO-GO E2 SSH/transfer/build/deserialization/model inference/benchmark in this entry**; the real copy result is accepted, and the next device gate will use this reviewed source bundle. Missing power telemetry does not block source correctness preparation.

## E2L1-013 — source runner integration repairs before operator dispatch

Review 2026-09-17: implementation `05624d7333966de0c16e1a33a79359fb0a3663dc`, L1A-012 `bbdcdb4fd41cf00f55f5bee0828c51d33d2e33a8`. Astra read the complete runner and tests and independently reran **74/74 edge tests PASS**. No frozen-model forward/export, server or device execution was performed. The six new tests mostly substitute FakeSourceRuntime; the claimed producer-equivalence test compares two packing helpers on already640x640 pixels, not actual Ultralytics preprocessing. These tests do not yet establish the real runtime boundary.

**HOLD the current L1A-012 server command at05624d7.** The conditional E2L1-012 execution prerequisites are not satisfied by this implementation. Do not install local packages or run the current code on server to rediscover these defects. Accepted E2 CUDA copy remains complete. Fix the three groups below together, with tests through the actual runtime class; no study or tolerance redesign.

### B1 — repair actual runtime and exporter integration

- `REQUIRED_MODULES` omits NumPy. `preprocess` imports it temporarily but never assigns `self.modules['numpy']`; `_numpy_tensor` and `ort_forward` unconditionally require that key. Astra directly reproduced `KeyError: 'numpy'` without running a model. Add NumPy to the prepared dependency/version contract and exercise actual tensor conversion, native-result packaging and ORT dispatch with injected model/session doubles, not a replacement SourceRuntime.
- Export path is inconsistent with installed Ultralytics8.4.102. Its exporter derives `self.file` from `model.pt_path`, then writes `self.file.with_suffix('.onnx')`; project/name does not implement the output layout assumed here. The private checkpoint currently lives in `private/checkpoint/best.pt`, so an ordinary successful export would yield its sibling ONNX and then fail the runner's `private/onnx_export` containment check. Put a hash-verified export checkpoint copy inside the intended export workspace OR explicitly accept and bind the exact expected sibling within the owned private root. Do not weaken containment to arbitrary paths or move/overwrite shared best.onnx. Test a faithful exporter double that follows pt_path semantics; assert source/private-copy before/after hashes and exact resolved output path.
- Explicitly load/evaluate the independent native model as CPU float32, validate actual YOLO11n head identity/nc/class order/end2end/export/training/xyxy flags and decoded output semantics before accepting references. Recording flags is not enforcing them. Preserve a separate export object; inspect and record exporter fusion/flags separately. Validate actual ONNX names `images`/`output0`, opset17, float32 static shapes and absence of Q/DQ/NMS wrappers, not shape alone. No fallback to a different export configuration.

### B2 — enforce the promised environment and tensor binding

- Current `ULTRALYTICS_AUTOUPDATE=false` is not the installed auto-install control: inspected utils source uses `YOLO_AUTOINSTALL`. Set that false before any Ultralytics import, disable automatic downloads/package operations, and assert the observed setting. Validate exporter-required dependency versions up front; fail before model calls when requirements are missing/incompatible. Pin Ultralytics8.4.102; freeze all observed source-environment versions, paths and relevant source hashes in a pre-dispatch record. Do not imply that merely find_spec success proves compatible versions.
- Apply actual CPU resource bounds with Torch thread settings and ORT SessionOptions (intra-op2, inter-op1), retaining CPU-only providers and hidden CUDA. Environment variables alone do not configure ORT's default thread pool. Tests must inspect the actual constructed options/provider list.
- `--commit` currently only echoes caller text. Bind the executing code worktree's actual HEAD plus script/helper hashes and verify the requested commit, separately from source_root's data checkout. Refuse changed executable source or mismatched requested commit; do not label an arbitrary string observed provenance. Detached worktree routing remains suitable.
- Freeze one actual contiguous little-endian float32 array per image, and derive BOTH saved bytes and runtime input from it. Currently payload and value are computed through separate paths; assert byte equality before both forwards and after each consumer, rather than assuming they agree. Record decode/LetterBox options, original/resized dimensions, padding, RGB conversion and input hashes. Test the real runtime preprocessing with deterministic non-square synthetic images using installed OpenCV/Ultralytics/NumPy, including padding and channel reversal; compare the helper where it is claimed equivalent. No frozen checkpoint inference is needed for these tests.

### B3 — correct numerical counts and preserve provenance on failure

- `compare_source_outputs` reports len(bounded_examples) as failing_elements. Astra compared two arrays with **58,800 actual mismatches and obtained reported20**. Count ALL mismatches independently of the example cap; include separate box/score counts and maxima. Test more than20 failures, failures confined to each channel domain, near-zero reference, and nonfinite payloads. Keep the existing equation unchanged. Validate finite bytes rather than trusting stale record metadata when reloading, and verify saved reference byte length/hash before use.
- A numerical FAIL currently raises before writing manifest/environment/records/ONNX hash; only a minimal failure record with comparisons survives publicly. Persist a complete terminal evidence record for execution-completed-but-numerically-failed runs: environment, model semantics, hashes, all input/output bindings, comparisons, private inventory and counters. Distinguish numerical verdict from execution status. Partial failures need the available provenance too; no private tensor/binary bytes in Git.
- Protect original checkpoint AND fixtures before/after on success and failure; the current original checkpoint has no post-run hash check. Record checks that cannot complete as unavailable, not unchanged. Fixture attempted counters must be set before verification. Export-internal counters must survive export exceptions rather than remaining0 because export() never returned. Use shared state/callback/error evidence, and distinguish attempts from completions. Persist report/log/index consistently for failures, and preserve original errors if a terminal artifact writer fails.

### Exit gate and updated operator handoff

GO local B1–B3 implementation, actual-runtime-boundary CPU tests and protocol correction. Existing dependencies suffice for NumPy/Torch/LetterBox tests in Astra's local measurement environment; mock only missing ONNX/export/session external calls while keeping production adapters/writers intact. No local dependency installation or frozen-model calls. Add end-to-end coverage through the real runtime class (not solely FakeSourceRuntime) for the failures above and the normal three-fixture path.

Append **L1A-013** with exact fixes/tests, actual-versus-mocked boundary and implementation commit; push this unchanged inbox and scoped files using NADUNGVN. Once B1–B3 critical tests and all edge regressions pass, the original frozen inputs/options/equation remain unchanged and an existing complete server environment is verified, **conditional GO ONE user-operated foreground CPU source run** under E2L1-012. No additional report-only gate is necessary for exactly these repairs; any different reference/export design or unmet test requires review. Main Luna's work is independent.

Replace the old05624d7 command with the exact newly pushed executable commit, resolve HEAD in the detached worktree, and check absent worktree/output paths before creating anything. Use the lab's sanitized Git invocation if LD_LIBRARY_PATH/LD_PRELOAD would affect Git. Keep source_root pointing to existing server data and the interpreter in the existing main-server environment, not an unmaterialized worktree dataset. Output remains `results/edge_readiness_v1/e2l1-012-source-v1` ONLY if absent; if anyone has already run the old command, preserve its output and report before choosing a new attempt path. No cleanup/reset, automatic retry, nohup, GPU-idle wait or package installation.

User runs server, pushes only public evidence; Luna1 audits then stops. No E2 SSH, transfer, build, inference or benchmark is authorized here. Astra leaves this entry unstaged for Luna1 commit/push; no application code was changed by Astra.

## E2L1-014 — accept source bundle with a disclosed strict failure; implement the target model diagnostic

Review 2026-09-17: corrected source artifact `34a542b2f787d7ef60dc3d3125cecad78e0c16f9`, report branch `41fa3ed`, executed code `7dd178af4c001d074d1efbd4bbbafcb767dc6263`. Astra read the five canonical public reports, counters, input/output bindings and source numerical records and reran **83/83 edge tests PASS**. Private binaries are not local; their content hashes remain server evidence to verify before transfer. Preserve the earlier pre-forward failed run and corrected run. Record the intervening buffer-type fix/operator approval as an actual attempt history, not a single uninterrupted run.

**ACCEPT the source bundle as completed, provenance-bound DIAGNOSTIC input, not source-equivalence PASS.** Source strict numerical verdict remains FAIL, with00006/00009 passing and00028 having one box mismatch out of58,800 values. Reference0.002925872802734375, observed0.002941131591796875, error1.52587890625e-05 input pixels versus allowance1.0292587280273439e-05. Flat16402 maps to channel1/anchor8002 for `[1,7,8400]`. All score comparisons pass under the existing equation. This is small in absolute units, but the affected anchor's confidence/detection impact is not in the public report; do not claim it is irrelevant or excluded without reading the retained tensors. This is not evidence that every export or device output is equivalent.

### Reviewer scope amendment

Do NOT widen tolerance, re-export or rerun source to obtain PASS. The next engineering question is whether E2 TensorRT reproduces this EXACT frozen ONNX on these exact inputs, with export drift separately visible. A source strict failure does not prevent implementing that independent diagnostic, but it prevents claiming end-to-end source equivalence from target-vs-ONNX success alone. This is an explicit post-result scope/gate qualification, not a historical PASS or original preregistration.

GO local implementation/tests/protocol for one E2 model-smoke workflow using the retained source ONNX SHA256 `bd20b36d640c502358c44edbbde51f05267eda2518b0c0a4cfe84ca18a00d4b7`. No source model/export call is needed. Candidate target workload for later review is **one E2-native FP16-enabled TensorRT8.5.2.2 build and three synchronized inference enqueues**, one each for00006/00009/00028, no warmup/repeats/latency/energy benchmark. Do not claim every layer executes FP16 just because that builder flag is enabled; keep float32 input/output bindings as required by the accepted adapter. No INT8/calibration cache, source TensorRT10 engine, precision-head sweep or other edge device.

### Implementation contract

1. Consume a manifest-bound private bundle: exactly the accepted ONNX plus three input, three native-reference and three ONNX-reference tensor files (10 binary files total), with paths/sizes/hashes from the canonical source manifest. Never transfer the copied checkpoint, whole repository, secrets or arbitrary manifest paths. Generate an explicit export/transfer allowlist and protect against traversal/symlink/path substitutions. The user retrieves server-private artifacts into a scoped local staging area; Luna1 does not SSH SERVER-01. Include exact operator retrieval/packaging instructions; model binaries are transferred privately, never committed to Git. No transfer under this implementation-only entry.
2. Validate ONNX/checksum/runtime/target identity, binding name/order/location/shape/dtype, CPU reference layout and E2 resources before any target call. Build only from that ONNX with the installed TRT8.5 parser/API. Record all parser errors; no fallback/re-export/package install. Choose and document one fixed bounded builder workspace (candidate1GiB), flags and logger lifetimes, new engine/output paths, no shared timing cache and no rebuild retries. Workspace cap does not bound total driver/builder memory. No device reset, clocks/power change or interference with another user's workload.
3. Connect the existing concrete CUDA owner/provider/adapter to the real parsed engine. Use exact frozen input bytes with no image decode/preprocess on E2 in this smoke. Correct H2D/enqueue/completion/D2H/lifetime handling, full finite output validation, per-stage attempted/completed counters and cleanup on every failure. One context/owned stream and sequential three enqueues; no unrecorded shape-probe inference/warmup. A failed enqueue stops the device run rather than retries. Preserve successful partials and unknown execution state after disconnect/timeouts.
4. Emit **three distinct comparisons**: (a) immutable source-native vs source-ONNX strict result from the accepted bundle; (b) E2 output vs source-ONNX reference; (c) E2 output vs source-native reference. For(b)/(c), retain the already frozen target box/score tolerances from e2_output_compare.py, reference on the correct side of the equation; declare these engineering thresholds, not accuracy guarantees. Never let(b) PASS erase(a) FAIL. Record all-element and channel counts/maxima plus bounded failures and the00028/channel1/anchor8002 neighborhood/scores from existing retained tensors. No score cutoff/NMS/AP/new tolerance choice to force a pass.
5. Add protocol/status fields distinguishing source strict failure, target diagnostic completion and target comparison outcome. Default device execution remains disabled. Proposed output `results/edge_readiness_v1/e2l1-014-model-smoke-v1` must be fresh; engine/full tensor outputs remain private, public manifests/report/logs/hash inventory only. Bound build and inference child deadlines independently; no global kill/cleanup. Missing energy telemetry is irrelevant here. Record shared-device state honestly without claiming isolated latency from a correctness smoke.

Tests must go through the production parser/builder/owner/adapter workflow with injected external TRT/CUDA calls, not replace the whole target stage with unconditional success. Cover parse/build failure, wrong bindings/runtime/hash, reversed binding order, nonzero distinct outputs for three inputs, source FAIL coexisting with target PASS, second-image enqueue failure, copy/sync/free failure, output-root collisions, tampered reference bytes and timed-out/unknown dispatch. Enforce Python3.8 compatibility for the complete E2 import/write path. Keep source and target code/environment roles separate; don't attempt to install Torch/ORT/onnxslim on E2.

### Handoff and gate

Append **L1A-014**, update the executable workflow and protocol, and push this unchanged inbox plus scoped code/tests through NADUNGVN. Include a proposed exact source-staging/transfer manifest, device command, resource limits and model-smoke comparison policy. **GO local implementation only; NO-GO server retrieval, E2 SSH/transfer/build/inference until Astra reviews the new target workflow.** No additional allocation smoke, read-only inventory rerun, source inference/export or benchmark. This is forward progress toward the first real model result, not acceptance of deployment accuracy. Main Luna's full-dev bridge preparation proceeds independently. Astra leaves this handoff unstaged for Luna1 commit/push.

## E2L1-015 — repair target dispatch, enforceable deadlines and failure evidence

Review 2026-09-18: executable `d9423f21d24ff0217e9424c9d6b7037602fd3596`, report `8976178bb9a232c1e4ac573f157cc80c80ab057f`. Astra independently reran **94/94 edge tests PASS** and inspected the new workflow. These are CPU/mock results, not real TensorRT/CUDA execution. The three comparison domains, default-disabled dispatch, finite/reference checks and private-binary boundary are retained. **GO local repairs below; HOLD the proposed real-device command.** This is independent of main Luna's authorized CPU dev bridge.

### T1 — correct the source/target boundary and bind accepted artifacts

The L1A-014 candidate runs `/home/ubuntu/.../local/g0_size_env/bin/python` on SERVER-01 but labels the run `--target E2`. That flag does not perform SSH or move execution to E2. The current preflight hardcodes target_id/hardware_profile and merely records hostname/machine; it only enforces a TensorRT version prefix. Replace the candidate with separately named source-staging (user on SERVER-01), local transfer, and **E2-side Python 3.8** execution steps. Luna1 may later operate E2 through the known SSH alias, but no SERVER-01 SSH authority is added. No command should execute the target builder in the server Python environment.

Enforce the existing observed target binding before loading TensorRT/CUDA: E2/SSH alias nx, expected host `arar-desktop`, aarch64, actual Jetson Xavier NX model identity from the recorded platform/device-tree source, and the accepted runtime profile. Distinguish expected from observed identity. Use existing infrastructure/probe records; do not invent a serial or require new isolation rules. Tests must reject an x86/server or wrong Jetson host even when an injected module reports TensorRT8.5.2.2. Record actual executing code revision and helper hashes; a caller-supplied label is not observed provenance.

Also freeze the canonical source manifest SHA256 from accepted artifact commit `34a542b2f787d7ef60dc3d3125cecad78e0c16f9`, `results/edge_readiness_v1/e2l1-013-source-v2/public/manifest.json`, using raw Git bytes. Currently only the ONNX digest is pinned; input/native/ORT tensor hashes are trusted from the supplied, unpinned manifest. Binding an edited manifest to equally edited tensors can therefore change the reference while all internal hashes agree. Check the pinned canonical manifest before deriving the ten-file allowlist; retain byte hashes for each private file and use the original manifest unchanged after relocation. Test jointly altered manifest/reference bytes and a wrong manifest with the same ONNX. No need to retrieve private data to implement this check; synthetic tests must be identified as such.

### T2 — deadlines must bound real blocking calls; counters must describe observed stages

`StageBudget.check()` is called before/after synchronous `build()` and `infer()`. It detects elapsed time only AFTER the call returns; it cannot bound a hung parser/build/enqueue/copy/sync/deserialization. The timeout test directly raises an injected exception, so it does not establish an enforceable deadline. Implement the previously requested owned child-process lifecycle with independent build and inference deadlines (include engine-load and cleanup in the relevant bounded scope). The CPU-only parent must receive durable stage events/partial evidence, stop only its own child/process group on deadline, and never retry or kill unrelated workloads. Validate timeout values as finite positive values before dispatch. Preserve unknown completion when dispatch occurred but completion was not observed; record termination/cleanup confirmation separately, without claiming guaranteed GPU cleanup.

Add a genuine CPU subprocess test whose stub blocks beyond the deadline without raising TargetTimeout itself. Verify bounded parent return, persisted timeout/failure evidence, no retry, no unintended second stage, and no unrelated process termination. Cover build and inference/load boundaries and reject NaN/Inf/zero/negative timeout configuration before any calls.

Counters currently mark parse/build attempted together and completed together only after all of build succeeds. Enqueue completed is recorded only after the whole adapter infer returns, and output-copy attempted is recorded after that copy already occurred. This loses the distinction between parse failure, build failure and enqueue-success/D2H-failure. Emit stage events from the production parser/builder/adapter boundary OR rename coarse counters honestly and explicitly mark fine-grained completion unknown when unavailable. Do not manufacture exact parse/enqueue/copy counts from outer function return. Tests: parse success/build failure; enqueue success/copy failure; timeout after dispatch. Keep the maximum budget one build and three application enqueues, no warmup/retry/benchmark.

### T3 — preserve primary errors and attempt all owned cleanup

Astra directly reproduced `_OwnedTargetExecution.close()` with an injected failing `provider.close()`: only `buffers.free` was called; neither `stream.close` nor `cuda.close` ran. The same uncaught provider-close path exists during partial open cleanup. In addition, a cleanup exception raised in the runner's finally block masks the original inference failure.

Attempt cleanup of every successfully acquired owned resource in appropriate order even when an earlier cleanup fails, collecting per-resource outcomes. Preserve the original parse/load/inference/copy error as the primary error and cleanup failures as separate evidence; when cleanup alone fails, report that as the primary failure. Persist partial target outputs and counters, without claiming successful teardown. Add tests through the actual runtime/runner for provider-close failure, partial allocation/open failure, and inference failure PLUS multiple cleanup failures. Do not solve this by swallowing exceptions or overwriting the first failure artifact.

### Delivery and gate

Keep FP16-enabled/1 GiB workspace, frozen ONNX/input bytes, three images, output contract and both numerical policies unchanged. Do not optimize, install packages, re-export the source or reinterpret strict FAIL. Update the protocol and replacement target-side candidate commands together; supersede the erroneous SERVER-01 target command explicitly. Append **L1A-015** with fixes, real-vs-mock test boundaries and remaining deployment prerequisites. Push scoped changes and this unchanged inbox using NADUNGVN; Astra does not push.

**GO CPU/local implementation and tests; NO-GO server retrieval/transfer, E2 SSH/build/deserialization/model inference or benchmark in this entry.** Return the repaired implementation for one combined review. Missing power telemetry does not block this correctness smoke, and no new VRAM/desktop isolation gate is introduced. The objective remains the first bounded real E2 model diagnostic after these executable-boundary defects are closed, not another allocation-only smoke. Astra leaves this entry unstaged for Luna1 commit/push.

## E2L1-016 — close reproduced packaging and production-child defects

Review 2026-09-18: executable `1603732675d4759b030a7d405b3a11aec49e2af7`, report `72b050dbb730027643db62312e5e316a34f79dea`. Astra reran **16/16 focused tests and 99/99 edge tests PASS** in the existing local measurement environment. No real CUDA/TensorRT import, model execution, SSH or device operation occurred. Identity-before-runtime validation and all-resource cleanup iteration are improvements. However, independent checks reproduce the blockers below; the shipped candidate cannot yet be executed successfully. **GO local repairs; NO-GO transfer/build/inference remains.** Keep all numerical contracts unchanged.

### P1 — correct the canonical manifest binding, with a real-artifact regression

The hardcoded `CANONICAL_SOURCE_MANIFEST_SHA256=df2e81e943c471cb540dd837151e87f7799510b01c050be1b2d14e2869f760be` is NOT the hash of the cited accepted Git blob. Astra computed SHA256 directly on subprocess byte output of:

`git show 34a542b2f787d7ef60dc3d3125cecad78e0c16f9:results/edge_readiness_v1/e2l1-013-source-v2/public/manifest.json`

The canonical blob is **20,346 bytes**, SHA256 **`60744680973a73d2986bdf59ce3c6bc956119aeeebbd2106960df57947665c08`**. Its status is execution_complete_numerical_fail and ONNX hash remains bd20b36d640c502358c44edbbde51f05267eda2518b0c0a4cfe84ca18a00d4b7. An LF-to-CRLF variant hashes to a5cf00f6d344db551d481a512edf4e8bf7d961047394af97b3b218e5fff2db32, also not the hardcoded value. Do not explain the observed mismatch away as CRLF.

Recompute independently, fix the binding and document the origin of the erroneous value if recoverable. Add a regression asserting the production constant against the accepted raw Git blob without patching either hash. Existing synthetic make_bundle tests replace the constant and cannot catch this defect. Transfer the original accepted manifest bytes without rewriting JSON; private tensor validation remains required. No new source export or reference generation.

### P2 — make the proposed code package executable and verifiable

The candidate uses `git archive` to send source, which has no `.git`, but `code_provenance()` requires `git rev-parse HEAD` in that directory. Astra reproduced CODE_REVISION_UNAVAILABLE for that condition. It also reproduced CODE_REVISION_MISMATCH when requested `1603732` is compared to the correct full 40-character HEAD. These are pre-device integration failures, not E2 numerical failures.

Choose one coherent supported route: a scoped archive with a canonical export manifest of reviewed Git revision and per-file hashes verified on E2 (label this archive provenance, not observed Git HEAD), or an actual correctly materialized detached checkout whose required files and full revision are available. Do not send credentials, the whole data repository or private checkpoints. Verify executable bytes against the reviewed package, not merely record their current hashes next to a caller revision. Include this evidence in public plan AND terminal manifest on success and failure; currently success drops code_provenance and the plan omits it.

Provide a complete local CPU rehearsal of the actual chosen package/CLI in a fresh directory outside any Git worktree, or of the selected real-checkout route. It must exercise production provenance before mocked device boundaries, use the exact commit format the command expects, and reject changed helper bytes. Keep source staging by the user on SERVER-01 separate from Luna1's later E2 operations. Replace placeholder target/staging commands with concrete paths from existing inventory, or identify the one genuinely missing operator input; do not present placeholder commands as ready to execute.

### P3 — exercise and repair the production parent/child path, not only its fallback

The current end-to-end tests pass ProductionFakeTarget, so `isinstance(self.target, E2TensorRTRuntime)` is false: they bypass `_run_subprocess_workflow`, `SubprocessStageExecutor` and the actual child entry. Add coverage of that branch and child entry using injected external TRT/CUDA interfaces or test bootstrapping, without a public switch that enables arbitrary engines or bypasses identity on real runs.

Concrete defects found:

- Successful `TensorRTOnnxBuilder.build()` overwrites last_event and discards parser_completed. Astra exercised the real builder with FakeTensorRT and got `last_event.get('parser_completed') is None`; the build child therefore reports parse_completed=0 for a successful parse/build. Build attempted is still incremented before parse success, and parse-success/build-failure does not preserve parse completion. Record events/counters at actual stage boundaries.
- The child observer increments in-memory counters but does not append them as events; a timeout loses intermediate dispatch/completion evidence. Persist observed enqueue/copy/load/build events as they happen, with explicit semantics: enqueue return true is dispatch acceptance, not proof of synchronized GPU completion.
- Parent copies child counters and event paths only on success. Failed/timeout inference can leave top-level counters at zero even after real enqueues. `_run_subprocess_workflow` initializes target_outputs to `{}`, so the outer failure recovery guarded by `"target_outputs" not in self.evidence` does not collect existing partial outputs. Reconcile durable child events/results and preserved private outputs on every exit; distinguish observed zero from unknown, and include their hashes/metadata in publishable evidence. Do not require pushing private tensors to audit a partial failure.
- `_raise_child_failure` can replace an outer timeout/unknown result with a generic ModelSmokeError when a child result file happens to exist, dropping termination evidence. Preserve parent deadline/termination facts independently from child result/error facts. Public inventory must include the relevant stage evidence (or embed its content/hashes), not just private event-file paths inaccessible after Git publication.
- Cleanup attachment only covers AdapterError in several paths. Retain cleanup outcomes for generic exceptions too. The non-subprocess fallback inspects sys.exc_info INSIDE the cleanup except, where it sees the cleanup exception rather than the original operation error, and can suppress a cleanup-only failure. Use explicit saved primary-error state consistently or remove redundant fallback production semantics; do not report success with failed cleanup.

Required bounded CPU tests: child build success with parse/build counts 1/1; parse success then build failure; full production parent/child success for three distinguishable outputs; second-image failure with first output preserved; enqueue accepted then copy/sync failure; timeout after a durable event; generic primary exception plus multiple cleanup errors; cleanup-only failure. Preserve source FAIL even when target comparisons pass. Check actual terminal public artifacts, not only internal objects.

### P4 — finish the deadline contract

The first communicate is bounded, but the timeout fallback calls unbounded `process.communicate()` after killing only the direct process. On POSIX, descendants that ignore SIGTERM and retain pipe handles can keep that wait open. SIGTERM being sent also does not itself prove termination, despite the current termination_confirmed assignment. Keep TERM and KILL escalation scoped to the owned group/session on E2, bound all wait/drain stages, and record observed child/group termination separately from signal dispatch. If teardown cannot be confirmed within the bound, return unknown/cleanup_unconfirmed without retry or unrelated-process actions. Cover this with a bounded CPU test (POSIX-specific where required, explicit skip on Windows); do not claim Windows execution proves Linux process-group behavior.

### Delivery and gate

Append **L1A-016** with all four repairs, independently computed canonical manifest hash, package rehearsal, production-child tests and any platform-specific skips. Run the full edge regression with the available local environment; keep failures outside this suite separately identified. Push scoped implementation/protocol/tests and this unchanged inbox using NADUNGVN. The accepted source data, FP16/1 GiB/three-enqueue design, source strict FAIL and target tolerances are unchanged. **GO local CPU repair/rehearsal only; NO-GO real E2/server transfer/build/inference until combined review.** No additional research experiment or allocation smoke is requested. Astra leaves this entry unstaged for Luna1 to commit/push.

## E2L1-017 — bounded final repairs, staging GO, conditional one-smoke authorization

Review 2026-09-18: code `6cce03e7125dd507702f168f98ec99fe0a8189c8`, report `6f75b42b732b0471e57f8af9258131df769816cb`. Astra independently ran **104/104 edge tests PASS** in the existing measurement environment (the99/99 figure belongs to the preceding code revision, not this regression). The corrected canonical source hash, archive-compatible provenance path, all-resource cleanup iteration, bounded TERM/KILL waits and public stage snapshots are accepted improvements. No actual source inference, TensorRT/CUDA, E2 SSH or device operation was performed by Astra.

There remain concrete lifecycle gaps below. Close them with focused tests, but do not restart scientific design or collect more allocation-only evidence. This entry separates staging from numerical execution and provides a conditional, bounded GO so a report-only permission round is not needed after exactly these repairs pass.

### C1 — child preflight must preserve its original failure

`_child_stage_main` assigns `builder = None` only AFTER code_provenance and runtime.preflight. Its except block reads builder even if either check failed first. Astra invoked the real child entry with an injected CODE_ARCHIVE_BYTES_MISMATCH: observed **UnboundLocalError: cannot access local variable 'builder'**, with no result JSON. Initialize cleanup/counter state before the try, and ensure every pre-runtime failure yields the original structured error with zero model calls. Cover provenance failure and identity/runtime failure through the actual child entry; no TensorRT/CUDA import is required for these tests.

### C2 — reconcile durable events, and test the real production lifecycle

The child now writes per-enqueue/copy events, but `_reconcile_stage_evidence()` only merges counters from result JSON, not from JSONL events. A forcibly timed-out child normally has no result file. The current timeout test supplies counters directly in an injected exception, which bypasses this real case. Parse complete valid event rows, reconcile observed counters and mark subsequent completion unknown; retain truncated final-row evidence without interpreting it as a completed operation. Test event-only timeout with no result file, one preserved output and at least one observed enqueue; verify terminal public counters/evidence, not only internal state.

Parser/build attempted counters still start together and parser completion events are appended after build returns. Record parse and builder dispatch at their actual boundaries or explicitly label late-derived evidence; a parser failure must not imply a builder invocation, and a build timeout must not erase observed parser completion. Keep enqueue acceptance distinct from stream-synchronized completion. Do not add hidden retries.

The current parent production-path test replaces the entire executor with ChildContract, and child-build tests replace the whole builder. Add one integration exercising SubprocessStageExecutor command construction, actual child entry, result/event files and parent terminal report using external TRT/CUDA doubles/test bootstrapping. This can remain CPU/mock and does not require private tensors or actual E2 runtime. Include success with three distinct outputs and second-image failure. Do not add a public arbitrary-child-command or identity-bypass option to production CLI.

Remove or fix the remaining fallback cleanup path that calls sys.exc_info inside the cleanup except; that still sees the cleanup exception rather than a saved primary failure and can suppress cleanup-only failure. Use explicit primary-error state as in the production child. Test generic primary plus cleanup failure and cleanup-only failure. Do not claim process-group/GPU isolation from a terminated leader; distinguish leader termination, group status if observed, and unverified CUDA teardown. The new bounded waits are useful without that overclaim.

### C3 — GO staging and existing-path resolution, independently of C1/C2

Luna1 may now perform **bounded read-only E2 checks via the existing nx alias** to resolve the installed Python3.8 executable, host/architecture/device-tree identity, writable owned staging location, disk availability and current competing workload. Existing E2 records already identify arar-desktop/aarch64/Jetson Xavier NX and Python3.8.10; resolve the actual executable instead of inventing one. No install/reset/configuration change, CUDA allocation or model execution at this stage. Record commands/outcomes without secrets. This is not a latency/isolation study; low-power desktop context and missing power telemetry do not create a new correctness blocker. Do not disturb other users' processes.

The source bundle originates on SERVER-01 at the recorded `results/edge_readiness_v1/e2l1-013-source-v2` root under `/home/ubuntu/Dung_TDTU/nighttime-tsd-new`. **The user alone operates SERVER-01.** Supply the user a read-only existence/hash check and narrowly scoped packaging command for the accepted manifest plus ten private files, using their recorded paths. If the recorded source root is absent, report that exact fact instead of searching/copying broadly. The user provides the resulting scoped archive through an approved transfer location; Luna1 may then transfer it locally to E2. No SERVER-01 SSH authority is granted to Luna1.

Allowlist remains exactly public/manifest.json; private/onnx_export/best.onnx; and00006/00009/00028.bin in each of private/inputs, private/native_reference and private/onnx_reference. No checkpoint, raw image collection, credentials or whole-repository copy. Retain original canonical manifest bytes (20,346 bytes; SHA25660744680973a73d2986bdf59ce3c6bc956119aeeebbd2106960df57947665c08) and verify every private binary hash before and after transfer. No source export/inference is authorized.

For code, use the eventual repaired full executable commit and a fresh scoped export from **canonical Git bytes**, not uncommitted/CRLF worktree files labeled with that commit. Generate the code-export manifest from that exact extracted package, independently compare its listed files to the commit's raw blobs, and record the manifest/package hashes locally before transfer. Include required package initialization/import files in the outer package inventory even where they are not in the six-helper provenance list. Recheck package and manifest hashes after transfer. This makes the trusted package origin explicit; create_code_export_manifest alone only hashes files it is given and does not prove they came from the supplied revision. No .git, credential or dataset tree is needed on E2.

Choose fresh owned paths, for example `/tmp/luna1-e2l1-017-code`, `/tmp/luna1-e2l1-017-source`, and `/tmp/luna1-e2l1-017-model-smoke`; assert absence before creating them, do not overwrite or automatically delete older attempts. Final commands must name the observed E2 interpreter and concrete verified paths. E2 archive execution uses --archive-manifest plus the same full40-character commit, not an abbreviated revision. Python3.8 import/compile and CPU/mock tests may be run on E2 against the staged code without importing real TensorRT/CUDA, to validate the actual deployment boundary. Do not install missing test dependencies; report and use existing lightweight doubles where appropriate.

### C4 — conditional GO exactly one real E2 model smoke

After C1/C2 fixes and their focused/edge regressions pass, the repaired code/protocol/report are pushed, the exact package/manifest/source hashes are verified, the E2 interpreter/identity are resolved and no incompatible resource/workload condition is observed, **Luna1 is authorized to execute ONE foreground E2 diagnostic**. No additional Astra response is required for these exact repairs and prerequisites. If any condition is unmet, report the concrete missing condition and HOLD the real run; do not silently weaken it. Any numerical design change, extra call, runtime mismatch or required installation returns to Astra review.

Unchanged budget: one FP16-enabled build from the accepted ONNX, workspace1GiB, followed by exactly one application enqueue per00006/00009/00028 in order, total3, zero warmup/retry/benchmark. Independent bounded build and inference child deadlines remain900s and180s; use the owned process-group termination protocol only for this run. Engine/native/full tensor bytes stay private. No INT8/calibration, new export, profiling, timing optimization, second device, full-dev capture or power/latency benchmark. Numerical policies and source strict FAIL remain unchanged; target PASS must not erase export_discrepancy.

On success or failure, preserve all public evidence and private partials. After a timeout/disconnect, inspect the existing owned run state read-only and report unknown completion; never rerun automatically. Stop after this one run and publish only public JSON/JSONL/Markdown/log evidence. The first real model result is an engineering smoke, not deployment-accuracy or paper-performance validation.

Append **L1A-017** with repair/tests and staging/dispatch prerequisites before execution, then an execution/artifact-audit addendum after the bounded run. Report actual versus mocked/POSIX-specific coverage honestly. Push this unchanged inbox and scoped work using NADUNGVN; Astra leaves it unstaged. **Immediate GO local repairs plus scoped staging/read-only E2; conditional GO one smoke only under C4; NO-GO benchmark or further experiment.** Main Luna proceeds independently with local two-model TensorRT feasibility preparation.

## E2L1-018 — staging handoff: connectivity and private-source archive, no new experiment

Review 2026-09-18: executable `b7fedfaf95bfbd9b1ee1248ec330ee5f7ac7d3f4`, report `f3bd42469fbfca9b448fdce88f42de901885cf8c`. Astra reran **106/106 edge tests PASS** and independently compared all six provenance helpers plus __init__.py in `D:/Research/luna1-e2l1-017-code-b7fedfaf95bfbd9b1ee1248ec330ee5f7ac7d3f4-blob` to the raw Git blobs of that executable commit: **7/7 exact**. Archive provenance verification succeeds without Git metadata; code-export-manifest.json SHA256 matches **aa136f72b9d395569e70944e1c67d1dbd1bdb23621912f9dfbed856297764507**. Local tests/package verification are not E2 numerical execution or proof of POSIX process-group behavior. Correct the inherited104/104 number when reporting this revision.

The concrete present blockers in L1A-017 are (1) nx SSH connection timed out, and (2) the ten private source binaries are not local. No real build/inference started. **Keep the existing E2L1-017 conditional one-smoke scope; do not create another code study or allocation smoke merely while waiting.** The production real-device subprocess path is the authorized path, not the mock fallback. Once connectivity/source and all C4 prerequisites are verified, that existing conditional authorization applies; no new scientific permission request is needed. Unresolved prerequisite/tests must still be reported rather than declared satisfied.

### User-operated SERVER-01 packaging

Send the user the following foreground commands, each as one line. They only package the existing accepted source bundle; no inference/export/build, Git push of binaries, or server SSH by Luna1. First inspect existence and canonical manifest hash:

```bash
(cd /home/ubuntu/Dung_TDTU/nighttime-tsd-new/results/edge_readiness_v1/e2l1-013-source-v2 && test "$(sha256sum public/manifest.json | cut -d ' ' -f 1)" = 60744680973a73d2986bdf59ce3c6bc956119aeeebbd2106960df57947665c08 && test -f private/onnx_export/best.onnx && for f in private/inputs/00006.bin private/inputs/00009.bin private/inputs/00028.bin private/native_reference/00006.bin private/native_reference/00009.bin private/native_reference/00028.bin private/onnx_reference/00006.bin private/onnx_reference/00009.bin private/onnx_reference/00028.bin; do test -f "$f" || exit 1; done && echo SOURCE_PRESENT)
```

Only after SOURCE_PRESENT, create one new scoped archive. The subshell's noclobber prevents overwriting an existing archive; tar lists exactly11 approved entries. If creation fails, preserve the partial and report; do not reuse it as complete or delete it automatically.

```bash
(set -C; cd /home/ubuntu/Dung_TDTU/nighttime-tsd-new/results/edge_readiness_v1/e2l1-013-source-v2 && tar -czf - public/manifest.json private/onnx_export/best.onnx private/inputs/00006.bin private/inputs/00009.bin private/inputs/00028.bin private/native_reference/00006.bin private/native_reference/00009.bin private/native_reference/00028.bin private/onnx_reference/00006.bin private/onnx_reference/00009.bin private/onnx_reference/00028.bin > /tmp/luna1-e2l1-017-source-v2.tar.gz) && sha256sum /tmp/luna1-e2l1-017-source-v2.tar.gz
```

The user supplies this archive and its printed SHA256 through an approved download/transfer location and tells Luna1 the local path. Do not put it in public Git/Git LFS. This packaging check does not replace validating all private bytes against the canonical manifest. Luna1 verifies archive hash, exact member allowlist, no symlink/traversal/directory escape, extracts to a fresh owned staging root, and uses the accepted source loader to check every binary before E2 transfer. If an archive member is a link or unexpected, stop; do not dereference arbitrary paths. Preserve raw manifest bytes and recheck hashes on the target.

### Connectivity and handoff

Ask the user to confirm E2 is powered/reachable from the lab network/VPN and whether its address changed. Do not assume a timeout means the device or SSH service is broken. No port scanning, network reconfiguration or unapproved host substitution. After connectivity is restored, a bounded read-only retry of the existing nx probe is permitted; this is not a model retry. Resolve actual Python3.8 executable and fresh owned paths before the staged command. Keep complete public probe stdout/stderr/exit evidence and the exact packaged executable commit.

Append **L1A-018** with source-archive state, connectivity state, verified package/source hashes and observed interpreter/path prerequisites. If still blocked, identify the user action needed; no repeated implementation rounds are required for a missing network route or missing private archive. If the existing C4 one-smoke conditions are fulfilled and the smoke runs, add its actual result/counters/comparisons/partials and stop for Astra review. No automatic retry after a model timeout, no benchmark or further device. Commit/push this unchanged inbox and scoped public evidence through NADUNGVN. Astra leaves this entry unstaged; no source packaging, transfer or SSH was performed by Astra.

### E2L1-018 addendum — reported archive hash received, local transfer still pending (2026-09-19)

The user reports creation of `/tmp/luna1-e2l1-017-source-v2.tar.gz` with SHA256 `bfbd48198faad06dc45a3c1c969e14ace3f1d7c34500ef4715d3bb6997edb28b`. Record this as the expected transfer hash, not as independently verified local bytes. The user still needs to download/copy that existing archive to an approved private local location and provide its actual path. A suggested location is `D:/Research/luna1-e2l1-017-source-v2.tar.gz`; this is a suggestion, not a claim the file exists. Astra checked that path and the matching Downloads path and neither was present at review time; no broad search or server connection was performed.

Once received, follow the existing E2L1-018 checks: archive hash, exactly11 approved regular-file members, no links/traversal, fresh owned extraction root, canonical manifest and all ten private hashes, then verified transfer to E2. Do not recreate the source bundle, commit private bytes to Git, or wait for main Luna's A2L-043. Keep E2 connectivity/runtime prerequisites explicit. The conditional one-smoke authorization in E2L1-017 is unchanged; this addendum adds no execution or retry budget. Append the actual receipt/verification/dispatch state to L1A-018 and let Luna1 commit/push this inbox with the scoped public report.

## E2L1-019 — Hugging Face transfer option; no SERVER-01 SSH dependency

2026-09-20: the user proposed pushing the source archive to a branch, then proposed Hugging Face instead. Proceed with preparation for a **private Hugging Face dataset repository as the transfer location**, keeping the archive outside the research GitHub repository. This is a transport change, not a new scientific experiment or a requirement to regenerate artifacts. HF supports private repositories and single-file uploads: https://huggingface.co/docs/hub/repositories-getting-started and https://huggingface.co/docs/huggingface_hub/main/guides/upload .

Resolve the user's HF namespace/repository URL first; GitHub account NADUNGVN does not establish their HF identity. Do not create a guessed repository or upload to a public destination by default. Use existing authorized authentication or ask the user to authenticate through the normal local/server mechanism; never request tokens in chat, place tokens in commands committed to Git, or log credentials. Confirm that the selected repository is private before transfer; a create-private flag alone is not evidence about an already-existing repository.

**Division of work remains unchanged:** Luna1 prepares exact foreground upload instructions; the user runs them on SERVER-01, where `/tmp/luna1-e2l1-017-source-v2.tar.gz` already exists. Luna1 then downloads from HF on local and handles the E2 lane. Do not ask for SERVER-01 DNS/IP/SSH credentials or scan the network: server SSH is not required for this route. Inspect available HF client/version before issuing client-specific commands; do not change the frozen inference environment just to upload a file. Use existing tooling or a separate transfer environment if the user approves its setup.

Upload only the existing archive after verifying its expected SHA256 `bfbd48198faad06dc45a3c1c969e14ace3f1d7c34500ef4715d3bb6997edb28b`, at a new scoped path such as `transfers/e2l1-017/luna1-e2l1-017-source-v2.tar.gz`. Do not upload a repository/dataset directory, credentials, other users' files or new model outputs. Do not overwrite an existing different object at that path. Record the HF repo ID, repo type, exact returned commit revision, artifact path, size and SHA256; download that pinned revision, not a moving branch head. The user does not need to download the file manually to Windows for this route.

After receipt, perform the same archive checks already authorized: expected hash, exactly11 approved regular-file members, no traversal or links, canonical manifest bytes and ten binary hashes. Then transfer the verified source and code packages to E2 and execute the existing conditional ONE smoke only once all E2L1-017 C4 prerequisites are met. No extra Astra response is required merely because HF replaces manual transfer. Main Luna's lifecycle fixes do not block this lane. Source strict FAIL remains recorded; reference outputs originate on SERVER-01, not Jetson.

Append **L1A-019** with actual transport status and any missing HF repo/auth prerequisite, then receipt/hash verification and smoke outcome when available. Do not claim an upload/download occurred from this planning entry. Keep the new inbox and earlier unstaged Astra entries intact when committing/pushing scoped docs via NADUNGVN; Astra has not uploaded, SSHed or run inference. Do not also publish the archive to a GitHub branch while preparing the HF route.

### E2L1-019 namespace resolved — DungJD (2026-09-20)

The user supplied https://huggingface.co/DungJD as their HF namespace. Astra opened the public profile and verified that the namespace exists; this does not establish local authentication or private-repository visibility. Use **`DungJD/nighttime-tsd-artifacts`**, repo_type **dataset**, visibility **private**, as the scoped transfer repository for this workflow. The name is a task-specific choice, not a claim that the repository already exists. No further namespace question is needed.

Luna1 may create this private repository using an existing authorized HF session for DungJD, or include its creation in the user-operated SERVER-01 upload instructions. Check the authenticated account without exposing credentials, inspect whether the destination exists, and confirm its private visibility before upload. If the existing destination is public, do not change its visibility or publish the archive automatically; report that collision and choose a new private destination with the user. Do not use the unrelated public drone-rocket dataset shown on the profile. If authentication is absent, request normal HF login on the relevant machine, not a token pasted into chat.

Destination artifact path remains `transfers/e2l1-017/luna1-e2l1-017-source-v2.tar.gz`. Give the user complete foreground commands to verify the existing archive and upload it from SERVER-01 with available tooling. After the upload revision is available, Luna1 downloads that revision, verifies the existing hash/member/private-byte contract, and continues the conditional E2 smoke independently. Do not wait for another Astra authorization just to create this private transfer repo, upload the approved archive, or download it. No repository creation, upload or authentication was performed by Astra in this addendum.

### E2L1-019 receipt addendum — SERVER-01 upload completed (2026-09-20)

The user confirmed the archive was uploaded from SERVER-01 under the authorized
`DungJD` account. Fixed receipt: private dataset repo
`DungJD/nighttime-tsd-artifacts`; HF commit
`0f909057a861a2b7fd887963a71c1a3f8f516007`; artifact path
`transfers/e2l1-017/luna1-e2l1-017-source-v2.tar.gz`; size `12430222` bytes;
SHA-256 `bfbd48198faad06dc45a3c1c969e14ace3f1d7c34500ef4715d3bb6997edb28b`.
The upload passed the hash guard and no overwrite was performed. Local pinned
download is still pending because the current local HF session is
`Dung-trivita`; no E2 build/inference has occurred.
